{
  "scripts": {
    "replay-ui": "node scripts/replay-ui-server.js",
    "build:analysis": "node scripts/build-index-react.js",
    "build:rail": "node scripts/compile-right-rail.js",
    "deploy": "npm run build:analysis && npm run build:rail && node scripts/embed-dashboard.js && cd worker && wrangler deploy --env='' && wrangler deploy --env production",
    "deploy:worker": "node scripts/embed-dashboard.js && cd worker && wrangler deploy --env='' && wrangler deploy --env production",
    "deploy:frontend": "bash scripts/deploy-frontend.sh",
    "deploy:all": "npm run deploy:frontend && npm run deploy:worker"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-react": "^7.23.0",
    "better-sqlite3": "^11.6.0",
    "wrangler": "^4.61.0"
  }
}

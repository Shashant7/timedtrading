# 2026-09-03 — npm arborist crashes with edgesOut=null on a clean install
# of this tree (vitest optional peer family + testing-library peers;
# npm/cli#9787). legacy-peer-deps keeps CI and local installs working.
legacy-peer-deps=true

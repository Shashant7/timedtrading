# July Recovery Iteration Log

Use this file as the authoritative append-only log for every recovery iteration from the 2026-04-05 save point onward.

## Logging Rules

- One row per meaningful change set or validation run.
- Always record the artifact bundle or run ID if a replay/backtest was launched.
- If no run was launched, record `run_id = none`.
- Never overwrite prior outcomes; add a new row instead.
- Explicitly note whether the large `RIOT` runner behavior was preserved, improved, or regressed.

## Columns

| Timestamp | Iteration | Type | Files / Surface | Summary | Run / Artifact | Outcome | RIOT Runner |
|---|---|---|---|---|---|---|---|
| 2026-04-05 | baseline-savepoint | replay | `worker/index.js`, `worker/pipeline/tt-core-entry.js`, `configs/julaug-golden-parity-v2-20260402.json` | Restored core runner-hold behavior, tightened early trims, reset leaked execution state, and recovered the large `RIOT` winner shape in the focused July lane. | `focused_replay_20260405-021006@2026-04-05T06:10:07.510Z` / `data/backtest-artifacts/focused-july-core-iter1-entrytrim--20260405-021006` | `22` closed trades, `16W / 6L`, `72.73%` WR, `$1,943.00` realized PnL. Remaining issues centered on `UNP`, `INTU`, `ORCL`, and one bad `RIOT`. | preserved |
| 2026-04-05 | autopsy-timestamp-fix | code | `worker/index.js` | Trade Autopsy replay payload now prefers lifecycle-history timestamps for `entry`, `trim`, and `exit`, reducing timestamp drift in live replay review. Also corrected fresh-entry soft-fuse suppression flow. | none | Timestamp display should now reflect actual trade lifecycle events more reliably in replay-scoped autopsy views. | preserved |
| 2026-04-05 | entry-quality-v2-broad-ltf-guard | replay | `worker/pipeline/tt-core-entry.js` | Broadened the existing opposed-lower-timeframe bull-entry conflict guard from only `HTF_BULL_LTF_BULL` to all `HTF_BULL_*` states, and added a new divergence-aware reject for bull entries that already show adverse `30m`/`D` RSI divergence plus bearish `15m` and `30m` EMA-cross structure. | `focused_replay_20260405-022732@2026-04-05T06:27:33.250Z` / `data/backtest-artifacts/focused-july-core-iter2-entryguards--20260405-022732` | Rejected. `INTU` and `ORCL` improved, but the broadened LTF-conflict block regressed `RIOT` badly: realized PnL fell to `-$1,772.47`, win rate fell to `68.42%`, and `RIOT` net PnL dropped to about `-$2,387.80`. | regressed |
| 2026-04-05 | entry-quality-v3-riot-protect | replay | `worker/pipeline/tt-core-entry.js` | Rolled back only the over-broad `HTF_BULL_*` expansion on the LTF-conflict guard, while keeping the more surgical divergence-aware bull-entry reject. | `focused_replay_20260405-023458@2026-04-05T06:34:58.999Z` / `data/backtest-artifacts/focused-july-core-iter3-riotprotect--20260405-023458` | Rejected. `INTU` and `ORCL` stayed improved, but `RIOT` still regressed badly (`-2443.05`) and total realized PnL remained negative (`-$1,358.62`). The run also showed a harness anomaly: only `5` created trades were reported while `36` trades were archived, so this lane should not be treated as a trustworthy promotion candidate. | regressed |
| 2026-04-05 | replay-harness-run-scope-fix | code | `worker/index.js`, `scripts/replay-focused.sh` | Hardened focused-run accounting so finalize only archives rows already stamped with the active `run_id`, and the focused replay manifest now reports deterministic run-scoped trade totals separately from the raw replay increment counter. | none | Removes the false created-vs-archived mismatch mode that was invalidating clean focused lanes. | preserved |
| 2026-04-05 | replay-harness-savepoint-rerun | replay | `worker/index.js`, `scripts/replay-focused.sh`, `worker/pipeline/tt-core-entry.js` | Restored `tt-core-entry` to the saved baseline logic, then reran the July core lane with only the harness hardening in place to verify clean accounting. | `focused_replay_20260405-091807@2026-04-05T13:18:08.704Z` / `data/backtest-artifacts/focused-july-core-savepoint-harnesscheck--20260405-091807` | Accounting is now clean: manifest, finalize response, and archive all agree on `21` total trades (`19` closed, `14W / 5L`, `73.68%` WR, `$423.20` realized PnL). However, this did not reproduce the earlier save point's `RIOT` runner; `RIOT` fell to `7` closed trades and about `$77.09` net PnL, so the remaining issue is real replay/parity drift rather than harness reporting noise. | regressed |
| 2026-04-05 | early-trim-first-gate | code | `worker/index.js` | Kept the global 3-tier first trim at `50%`, but tightened the fallback first-trim gate so non-price-driven trims now require the trade to be aged enough and materially progressed from entry toward the actual TRIM TP before they can fire. | none | Targets July's cosmetic near-entry trims without changing price-driven TP hits or the existing trim-size ladder. | preserved |
| 2026-04-05 | smart-runner-bootstrap-trim | code | `worker/index.js` | Added a bridge for untrimmed profitable trades where runner-style TD exhaustion says "hold": the engine now takes the initial `50%` trim and hands the trade into defend mode, instead of skipping the trim and only acting later once runner exits are already in play. | none | Restores the intended sequencing for cases like `GRNY`: trim first on the first exhaustion-style warning, then allow subsequent runner deferral/hold behavior. | preserved |
| 2026-04-05 | grny-journey-td-handoff-v2 | code | `worker/index.js` | Broadened the TD-exhaustion handoff so profitable untrimmed runners can bootstrap their first trim from the first qualified smart-runner exhaustion signal, and post-trim runners can still defer/hold when structure remains intact even if fuel is marked critical. | none | Targets the remaining GRNY journey gap: trim earlier in the move, then defer the later runner exhaustion instead of closing outright. | preserved |
| 2026-04-05 | replay-trim-persistence-and-logical-dedup | code | `worker/index.js`, `scripts/export-focused-run-artifacts.js` | Made trim timestamps/prices resolve from lifecycle history instead of trusting raw trade fields, and normalized replay/run inspection surfaces to dedupe same logical trades by `ticker + direction + entry_ts` while preferring the most complete lifecycle row. | none | Should stop false "no trim" journeys caused by missing `trim_price`, and reduce same-entry duplicate rows across run trades, autopsy, and focused artifact export. | preserved |
| 2026-04-05 | grny-post-trim-td-cooling-window | code | `worker/index.js` | Reworked the TD smart-runner support-hold path so freshly trimmed long runners can defer the first exhaustion-style close inside a bounded post-trim cooling window when 4H structure is intact, lookback trend is still holding, and giveback remains controlled. | none | Targets GRNY-style continuation after the first trim without turning exhaustion exits into a blanket no-op; validation still required. | preserved |
| 2026-04-05 | grny-posttrim-integrity-check | replay | `worker/index.js`, `scripts/export-focused-run-artifacts.js` | Deployed the trim-persistence, logical-dedup, and post-trim TD-cooling changes, then reran isolated `GRNY` for `2025-07-01` to `2025-07-03` to verify the journey payload and runner behavior. | `focused_replay_20260405-145152@2026-04-05T18:51:53.510Z` / `data/backtest-artifacts/focused-grny-jul1-3-posttrim-integrity-check--20260405-145152` | Integrity fix succeeded: the lane now exports exactly `1` run-scoped `GRNY` trade and preserves a real `trim_price` (`22.4443`) in both `trades.json` and Trade Autopsy. Behavioral target is still not met: the trade remains a modest winner (`$138.10`) and still exits via `SMART_RUNNER_TD_EXHAUSTION_RUNNER`, so more targeted runner-path tracing is needed. | preserved |
| 2026-04-05 | grny-td-runner-safety-net-v2 | code/replay | `worker/index.js` | Broadened the final smart-runner close safety net so `td_exhaustion_runner` can still defer into defend mode when HTF structure is intact and the move still looks like a healthy pullback/trend-hold, even if the strict cloud-expansion gate is not met. | `focused_replay_20260405-145753@2026-04-05T18:57:54.001Z` / `data/backtest-artifacts/focused-grny-jul1-3-posttrim-deferral-v2--20260405-145753` | Isolated `GRNY` improved: trim still occurs early (`trim_ts=2025-07-01 11:40`, `trim_price=22.4443`), and the trade no longer closes via TD smart-runner exhaustion inside the Jul 1-3 window. It remains an open trimmed runner (`TP_HIT_TRIM`) with `+$167.63` unrealized PnL at artifact capture, so the next check is whether the broader July lane benefits without introducing runner drift. | preserved |
| 2026-04-05 | first-trim-maturity-guard-v2 | code | `worker/index.js` | Generalized the first-trim maturity gate beyond the smart-runner bootstrap so the main TRIM-stage path also blocks an initial `50%` trim until the move has aged and shown real completion / Saty phase / ATR maturity, instead of taking a cosmetic near-entry trim on a barely-green bar. | none | Prompted by the failed `focused_replay_20260405-152136` live `GRNY` lane, where the shared `2025-07-01` trade still trimmed at `22.4443` just one hour after entry and later exited as a small `sl_breached` win. | preserved |
| 2026-04-05 | first-trim-direct-bypass-guards | code | `worker/index.js` | Extended the first-trim maturity guard into the direct low-profit trim callers (`SOFT_FUSE`, `RSI_DIVERGENCE`, `ST_FLIP_4H`) and added a hard minimum first-trim profit floor so defend/hold behavior can win over cosmetic `50%` partials. | none | Closed the remaining bypasses that were still trimming `GRNY` before the shared TRIM-stage gate could veto the action. | improved |
| 2026-04-05 | grny-jul1-3-firsttrim-maturity-guard-v6 | replay | `worker/index.js` | Replayed isolated `GRNY` for `2025-07-01` to `2025-07-03` after the direct-caller guards and profit floor were deployed, to verify the exact `Jul 1` shared entry journey. | `focused_replay_20260405-153811@2026-04-05T19:38:12.197Z` / `data/backtest-artifacts/focused-grny-jul1-3-firsttrim-maturity-guard-v6--20260405-153811` | Material improvement: the shared `2025-07-01 12:30 PM ET` `GRNY` entry no longer trims on Jul 1. First trim now lands at `2025-07-02 10:00 AM ET` at `22.485` with `trimmed_pct=0.5`, and the trade holds as an open runner through the Jul 1-3 probe with `+$178.56` unrealized PnL. | improved |
| 2026-04-06 | mainline-entry-guard-reanchor | code | `worker/pipeline/tt-core-entry.js`, `tasks/july-mainline-reconciliation-plan-2026-04-06.md` | Began reconciling `main` back toward the proven July package by restoring the speculative pullback divergence guard to the exact `422b606` savepoint logic while leaving hardening work intact. The only remaining post-savepoint delta in `tt-core-entry.js` is a trace-only `SOFI` probe case. | none | Mainline behavior-sensitive reconciliation is now underway on `main` itself instead of in side worktrees. No replay launched yet; this change removes one unvalidated entry-filter expansion from the July candidate path. | preserved |
| 2026-04-06 | mainline-direct-first-trim-guard | code | `worker/index.js`, `tasks/july-mainline-merge-matrix-2026-04-06.md` | Restored the missing direct-caller first-trim maturity guard on `main` by centralizing an initial signal-trim gate and applying it to `SOFT_FUSE`, `RSI_DIVERGENCE`, and `ST_FLIP_4H`. Unmatured first trims now defer or skip instead of forcing cosmetic partials ahead of the normal TRIM-stage logic. | none | Reconciles a proven later `GRNY` lifecycle fix back onto `main`. The direct bypass paths were still trimming on shallow profit thresholds; `main` now blocks those early signal-driven first trims unless the move has both profit and completion maturity. | improved |
| 2026-04-06 | mainline-autopsy-snapshots-and-peak-lock | code | `worker/index.js`, `tasks/todo.md` | Hardened Trade Autopsy snapshot persistence by awaiting exit-snapshot writes, backfilling `direction_accuracy.run_id` on close, allowing clean-lane autopsy reads to prefer exact `run_id` but still fall back to same-trade null-scoped rows, and making the default archived-run picker prefer the newest completed lane by creation time. Also added a post-trim peak-reaction stop ratchet so trimmed winners that tag exhaustion/target zones and start to give back tighten much faster. | none | Syntax/lint clean. This targets the two remaining gaps on top of the frozen v5 July checkpoint: missing entry/exit snapshots in autopsy and runners giving back too much after local peaks. Validation replay still pending. | preserved |
| 2026-04-07 | july-mainline-reanchor-v6-snappeakfix | replay | `worker/index.js` | Deployed the snapshot persistence + peak-reaction lock changes and reran the frozen July validation basket (`UNP, INTU, SANM, RIOT, B, ORCL, GRNY`) on the saved iter-1 config. | `focused_replay_20260406-200235@2026-04-07T00:02:36.138Z` / `data/backtest-artifacts/focused-july-mainline-reanchor-v6-snappeakfix--20260406-200235` | `8` run trades, `6` closed, about `$1,174.99` closed PnL and `$1,606.42` total. Closed trades in the archived autopsy now consistently carried entry snapshots and most carried exit snapshots; default Trade Autopsy selection resolved back to this completed run instead of drifting to an older one. A remaining hole was identified on the `TP_FULL` RIOT winner, which still lacked `exit_snapshot_json` and required one more full-close-path patch. | preserved |
| 2026-04-07 | riot-tpfull-exitsnap-proof | replay | `worker/index.js` | Patched the `TP_FULL` full-trim/zombie-close paths to use the same exit-snapshot persistence helper as the standard close lane, then reran a RIOT-only July window to verify the exact archived autopsy case that was still missing. | `focused_replay_20260406-201745@2026-04-07T00:17:46.910Z` / `data/backtest-artifacts/focused-riot-tpfull-exitsnap-check--20260406-201745` | Proof complete: both archived `RIOT` trades now return `signal_snapshot_json=true` and `exit_snapshot_json=true`, including the `TP_FULL` winner (`RIOT-1751902200000`). This closes the remaining snapshot persistence gap for the saved July recovery lane. | improved |
| 2026-04-07 | july-peak-lock-ab-proof | code/replay | `worker/index.js`, `configs/iter5-runtime-recovered-20260325-peak-lock-off-20260407.json`, `configs/iter5-runtime-recovered-20260325-peak-lock-on-20260407.json`, `data/backtest-artifacts/focused-july-peak-ab-variant-on--20260406-205957/peak-capture-ab-vs-v5.md` | Added a run-scoped `deep_audit_peak_reaction_lock_enabled` toggle so the post-trim peak ratchet could be tested honestly, then ran matched July control/variant focused lanes on the preserved basket and compared closed-trade giveback metrics back to `v5`. | `focused_replay_20260406-204912@2026-04-07T00:49:12.963Z` / `data/backtest-artifacts/focused-july-peak-ab-control-off--20260406-204912`; `focused_replay_20260406-205957@2026-04-07T00:59:58.563Z` / `data/backtest-artifacts/focused-july-peak-ab-variant-on--20260406-205957` | No validation gain yet: both lanes finished with identical `8` run trades, `6` closed, about `$1,174.99` closed PnL and `$1,606.42` total, no `PEAK REACTION` log hits, and no exit-quality delta on the comparable closed `INTU`/`ORCL`/`RIOT` trades versus `v5`. Keep the snapshot/read-model fixes, but treat the peak lock itself as unvalidated until trigger instrumentation or threshold retuning shows a real July delta. | preserved |
| 2026-04-07 | riot-pre-earnings-entry-block | code/replay | `worker/index.js` | Fixed replay/live upcoming-event lookup so ticker-specific earnings rows fall back to D1 when the cached event set is incomplete, stopped treating backfilled `status=resolved` earnings rows as automatically ineligible for pre-event guards, and added an execution-stage entry block for fresh positions when earnings are within the existing pre-event risk window. | `focused_replay_20260406-214130@2026-04-07T01:41:33.030Z` / `data/backtest-artifacts/focused-riot-jul30-preearnings-block-v2--20260406-214130`; `focused_replay_20260406-214727@2026-04-07T01:47:30.334Z` / `data/backtest-artifacts/focused-riot-jul30-preearnings-clean-v3--20260406-214727` | Validation passed on the clean lane: the `RIOT` Jul 30 to Aug 1 replay now seeds the Jul 31 earnings event and finishes with `237` scored snapshots, `0` run trades, and `0` replay trade increments. A non-clean rerun still surfaced stale archived `RIOT` rows despite `0` daily replay trades, so the behavioral proof should rely on the clean-slate artifact while the separate archive contamination thread remains tracked elsewhere. | preserved |
| 2026-04-07 | july-aug-earnings-loss-sweep | replay | `worker/index.js`, `tasks/todo.md` | Ran a broader strict July/Aug sweep of archived loss clusters opened on the earnings date or the prior trading day, then validated the new guard on clean focused lanes grouped by actual current event coverage. | `focused_replay_20260406-220034@2026-04-07T02:00:42.138Z` / `data/backtest-artifacts/focused-earnings-guard-latejuly-cluster-v1--20260406-220034`; `focused_replay_20260406-220346@2026-04-07T02:03:48.393Z` / `data/backtest-artifacts/focused-msft-jul30-preearnings-check--20260406-220346`; `focused_replay_20260406-220512@2026-04-07T02:05:15.123Z` / `data/backtest-artifacts/focused-earnings-guard-earlyaug-cluster-v1--20260406-220512`; `focused_replay_20260406-220635@2026-04-07T02:06:37.374Z` / `data/backtest-artifacts/focused-earnings-guard-nvda-aug26-v1--20260406-220635`; `focused_replay_20260406-220746@2026-04-07T02:07:47.953Z` / `data/backtest-artifacts/focused-earnings-guard-csco-aug13-v1--20260406-220746` | Strict same/next-day loss cases now look covered on clean lanes: `RIOT`, `CAT`, `ANET`, `NVDA`, and `CSCO` all finished with `0` run trades in their earnings windows, and the late-July basket only produced two `MSFT` losses, which decomposed into one unrelated Jul 28 loser plus a Jul 30 same-day earnings entry that also replayed to `0` trades when isolated. The sweep also exposed metadata caveats: some older archived “earnings-adjacent” rows (`PWR`, `AMZN`, `FSLR`) do not currently reseed any earnings event in the worker route, so they should be treated as stale-archive candidates rather than current behavioral regressions. | preserved |
| 2026-04-07 | earnings-seed-single-ticker-fallback | code/replay | `worker/market-events-seed.js` | Hardened the single-ticker historical earnings seeder so an empty Twelve Data per-symbol earnings response falls back to the date-range earnings calendar for the same ticker before declaring `0` seeded events. | `focused_replay_20260406-221733@2026-04-07T02:17:35.347Z` / `data/backtest-artifacts/focused-earnings-guard-pwr-amzn-fslr-v1--20260406-221733`; `focused_replay_20260406-222241@2026-04-07T02:22:44.953Z` / `data/backtest-artifacts/focused-pwr-seed-fallback-proof-v1--20260406-222241` | Root cause clarified and fixed: `PWR`/`AMZN`/`FSLR` already had Jul 31 earnings rows in D1 and their clean strict lane stayed at `0` trades, so the remaining issue was seeding robustness/reporting rather than guard behavior. After the fallback patch, the same focused replay path now reports `Ticker PWR: 1 earnings events` and still finishes with `0` run trades. | preserved |
| 2026-04-07 | july-aug-macro-loss-canaries | replay | `tasks/todo.md` | Queried repeated July/Aug loss clusters against the worker's high-impact macro set (`CPI`, `PPI`, `FOMC`, `PCE`, `NFP`) and validated a few representative clean lanes before changing behavior. | `focused_replay_20260406-223154@2026-04-07T02:31:56.894Z` / `data/backtest-artifacts/focused-macro-guard-nke-fomc-jul30-v1--20260406-223154`; `focused_replay_20260406-223258@2026-04-07T02:33:00.968Z` / `data/backtest-artifacts/focused-macro-guard-riot-ppi-jul14-v1--20260406-223258`; `focused_replay_20260406-223415@2026-04-07T02:34:17.385Z` / `data/backtest-artifacts/focused-macro-guard-unp-cpi-jul10-v1--20260406-223415`; `focused_replay_20260406-223520@2026-04-07T02:35:22.428Z` / `data/backtest-artifacts/focused-macro-guard-wmt-ppi-aug11-v1--20260406-223520` | Current macro handling looks materially healthier than the old archives suggest. `NKE` on FOMC day, `UNP` before CPI, and `WMT` before PPI all finished with `0` run trades. The one macro canary that did open (`RIOT` over Jul 14-15 PPI) entered at `2025-07-15 1:30 PM ET`, which is after the release, so it is not evidence of a missing pre-event macro block. Keep auditing if needed, but no data-supported reason to add an earnings-style macro entry blocker yet. | preserved |
| 2026-04-07 | halo-short-bull-reclaim-guard | code/replay | `worker/pipeline/tt-core-entry.js`, `tasks/todo.md` | Added a mirrored short-side reject so speculative short pullbacks are blocked when the lower timeframes are already reclaiming bullish structure (`15m` + `30m` ST bullish plus a fresh bull flip / bull EMA-cross impulse without fresh bearish confirmation). Deployed the worker and ran a small proof basket around the archived `HALO` Jul 3 bad short versus the contemporaneous `OKE` short control. | `focused_replay_20260406-235131@2026-04-07T03:51:33.971Z` / `data/backtest-artifacts/focused-halo-oke-short-reclaim-guard-proof-v1--20260406-235131` | The deployed runtime finished with `0` trades for both `HALO` and `OKE` on `2025-07-03`. Interval diagnostics showed both names are already blocked earlier by `tt_bias_not_aligned` because `m10`/`m30` are bullish while higher-TF short alignment is incomplete, so the new guard is best treated as explicit hardening of the same failure mode rather than a source of fresh behavioral lift under the current pinned config. | preserved |
| 2026-04-07 | intu-jci-runtimefix-proof | code/replay | `worker/pipeline/tt-core-entry.js`, `worker/index.js`, `tasks/todo.md` | Added direct runtime tracing for `INTU-1751388300000` and `JCI-1751388300000`, confirmed that `INTU` was still qualifying because `hasSqRelease=true` was being treated as enough pullback confirmation despite counter-trend lower TF structure, and confirmed that `JCI`'s DOA path was evaluating the same trade through a stripped replay position shell that sometimes dropped `maxFavorableExcursion`. Tightened the speculative long counter-LTF pullback reject so only structural reclaim signals count, and fixed replay management state handoff so `openPositionContext` carries canonical MFE/MAE/trim data plus a trade back-reference. | `focused_replay_20260407-005334@2026-04-07T04:53:37.324Z` / `data/backtest-artifacts/focused-intu-jci-proof-v4-runtimefix--20260407-005334` | Proof passed. The lane improved from `4` run trades / `2` closed losses to `2` run trades / `0` closed losses. `INTU-1751388300000` is gone, while `JCI-1751388300000` remains open at about `+1.67%` instead of closing as `doa_early_exit`. | preserved |
| 2026-04-08 | july-package-freeze-prune-sepdec-guards | code | `worker/pipeline/tt-core-entry.js`, `tasks/july-mainline-candidate-freeze-2026-04-08.md` | Reconstructed the July candidate path on `main` by removing the unresolved Sep-Dec weak-context experiment guards from `tt-core-entry` while preserving the validated `INTU/JCI` reclaim fix, then froze the exact package definition for the next July control lane. | none | `tt-core-entry` is back down to the validated July-focused delta set: trace hooks plus the `INTU/JCI` structural-reclaim reject. The weak-context long/short guards that over-pruned `AA`/`SANM` and collapsed the `CRM` sentinel branch are explicitly excluded from the frozen July candidate package. | preserved |
| 2026-04-08 | july-mainline-candidate-freeze-v1 | replay | `worker/pipeline/tt-core-entry.js`, `worker/index.js`, `scripts/export-focused-run-artifacts.js`, `tasks/july-mainline-candidate-freeze-2026-04-08.md` | Ran the first clean July sentinel control on the frozen candidate package using the pinned savepoint config and sentinel basket `RIOT,GRNY,FIX,SOFI,CSCO,SWK`. | `focused_replay_20260407-223540@2026-04-08T05:36:03.943Z` / `data/backtest-artifacts/focused-july-mainline-candidate-freeze-v1--20260407-223540` | Clean lane completed with `3` run trades, `2` closed, `1W / 1L`, `$315.16` realized PnL and one open trimmed runner. `RIOT` preserved a meaningful winner (`TP_FULL`, about `+12.12%`), `GRNY` remained an open trimmed runner with positive PnL, and the lane stayed free of non-scope contamination. `SOFI` still opened and lost, while `FIX`, `CSCO`, and `SWK` were absent in this sentinel window. | preserved |
| 2026-04-08 | july-mainline-equalscope-v1 | replay | `worker/pipeline/tt-core-entry.js`, `worker/index.js`, `scripts/export-focused-run-artifacts.js`, `tasks/july-mainline-candidate-freeze-2026-04-08.md` | Ran the first clean broad July equal-scope validation on the frozen candidate package using the protected focused-golden Jul/Aug basket from the archived reference universe: `XLY,PH,GRNY,MTZ,ETN,SWK,RIOT,ON,IESC,FIX,AGQ,SGI,CDNS,HUBS,ABT,KWEB`. | `focused_replay_20260407-225747@2026-04-08T05:58:21.429Z` / `data/backtest-artifacts/focused-july-mainline-equalscope-v1--20260407-225747` | The lane was clean but failed the broad validation gate. It finished with only `4` run trades (`3` closed, `1W / 2L`, `$170.71` realized PnL, one open `GRNY` trimmed runner), versus the archived focused-golden reference universe's `66` total trades / `59` closed / `43W / 13L`. Positive signs: `RIOT` still produced the meaningful `TP_FULL` winner and `GRNY` stayed positive as an open trimmed runner. Negative signs: the equal-scope candidate was still far too sparse, with only `XLY`, `MTZ`, `RIOT`, and `GRNY` appearing at all. This is a clean reproducible checkpoint, but not a July pass and not promotable. | preserved |
| 2026-04-08 | july-missing-basket-forensics-v1 | forensics | `data/backtest-artifacts/focused-july-mainline-equalscope-v1--20260407-225747/manifest.json`, `data/backtest-artifacts/focused-july-mainline-equalscope-v1--20260407-225747/jul1-fingerprint.json`, `tasks/todo.md` | Performed the first missing-basket forensic pass on the clean equal-scope candidate to isolate a concrete blocker cluster before changing code again. The strongest evidence is at the run-contract layer: the artifact manifest shows `skip_backfill: true`, `dataset_manifest: null`, and `tickerUniverseCount: 0`, while the Jul 1 fingerprint missed all target names (`0/4`, every target marked `missing_ticker`). | `focused_replay_20260407-225747@2026-04-08T05:58:21.429Z` / `data/backtest-artifacts/focused-july-mainline-equalscope-v1--20260407-225747` | First concrete blocker cluster isolated: this lane violated the validation contract's data-readiness requirement, so the earliest missing cluster is a basket/data hydration failure rather than a justified new entry-logic patch. The next honest step is to rerun July equal-scope with verified backfill/data coverage and a captured dataset manifest before reopening entry blockers. A secondary diagnostic note surfaced too: `/timed/admin/interval-replay` currently errors with `replayTradeScope is not defined`, so interval-first forensics are partially impaired until that admin route is repaired. | preserved |
| 2026-04-08 | july-mainline-equalscope-v2-dataset | replay | `scripts/replay-focused.sh`, `data/replay-datasets/july-equalscope-2025-07-01-2025-08-08/manifest.json`, `tasks/todo.md` | Built a basket-scoped frozen dataset manifest for the exact equal-scope window (`2025-07-01 -> 2025-08-08`) by backfilling only the protected 16-name July basket through the required 60-day coverage window, verifying those names were absent from the worker's candle-gap list, manually seeding macro/earnings events for the same window, and rerunning the lane against that manifest plus the pinned July config. | `focused_replay_20260408-002421@2026-04-08T07:24:23.599Z` / `data/backtest-artifacts/focused-july-mainline-equalscope-v2-dataset--20260408-002421` | Data-readiness enforcement materially restored the basket: the corrected lane finished cleanly with `13` run trades / `11` closed / `7W / 4L` / `2` open trimmed runners and about `$4,692.84` realized PnL (`$5,289.37` total run PnL), versus only `4` trades on the invalid `skip_backfill` lane. `RIOT` preserved the large `TP_FULL` winner, `GRNY` remained an open trimmed runner, and missing names such as `FIX`, `ETN`, `IESC`, `AGQ`, and `CDNS` now reappeared. However, July still does not pass broad equal-scope validation against the archived `66`-trade golden basket, and the Jul 1 fingerprint remains `0/4`, with `CDNS` now present but drifting to a later `tt_momentum` entry while `ORCL`/`CSX`/`ITT` are still absent. The next blocker cluster is therefore narrowed to early Jul 1 path/timing drift rather than raw dataset hydration failure. | preserved |
| 2026-04-08 | jul1-fingerprint-forensics-v2 | forensics | `scripts/check-jul1-fingerprint.js`, `data/backtest-artifacts/focused-july-mainline-equalscope-v2-dataset--20260408-002421/jul1-fingerprint.json`, `worker/index.js` | Audited the Jul 1 fingerprint contract against the corrected equal-scope lane and confirmed it is not scope-safe for this basket. The checker is hardcoded to `CDNS`, `ORCL`, `CSX`, and `ITT`, but the protected equal-scope replay scope only includes `CDNS` from that set. Direct worker diagnostics showed `ORCL`, `CSX`, and `ITT` had `0` Jul 1 `timed_trail` rows and were not part of the launched replay scope, so their continued absence cannot be treated as an equal-scope regression. For the one in-scope name, `CDNS`, the worker now has `79` Jul 1 rows, enters `in_review` on time at `2025-07-01T14:00:00Z`, and still creates a `tt_momentum` trade at `2025-07-01T14:10:00Z`, which is a narrow timing drift from the archived `2025-07-01T14:00:00Z` `tt_momentum` reference rather than a full miss. | `focused_replay_20260408-002421@2026-04-08T07:24:23.599Z` / `data/backtest-artifacts/focused-july-mainline-equalscope-v2-dataset--20260408-002421` | The `0/4` Jul 1 fingerprint should not be used as the next blocker gate for this equal-scope lane in its current form. The true remaining problem is now the in-scope basket still being too sparse overall (`13` vs `66`), not `ORCL`/`CSX`/`ITT` specifically. Future Jul 1 checks for this lane should either be scope-aware or limited to in-scope names such as `CDNS` and other protected-basket early entries. | preserved |
| 2026-04-08 | equalscope-in-scope-missing-cluster-v3 | forensics | `worker/index.js`, `worker/pipeline/tt-core-entry.js`, `tasks/july-mainline-candidate-freeze-2026-04-08.md` | Continued the corrected equal-scope forensic pass by diffing the archived golden basket against the restored `13`-trade lane and then tracing the dominant in-scope missing branch. The largest remaining cluster is `XLY` (`6` missing references). Isolated `candle-replay` probes for `XLY` on the missing July dates showed repeated entry rejection families (`tt_pullback_not_deep_enough`, `tt_momentum_30m_5_12_unconfirmed`, `tt_pullback_non_prime_rank_selective`), and Jul 1 additionally surfaced `tt_weak_context_rank_inflation_guard`. That last guard is the key ingredient because the frozen July candidate document explicitly excludes it, while the current workspace search no longer contains that reject path in `worker/pipeline/tt-core-entry.js`. | `focused_replay_20260408-002421@2026-04-08T07:24:23.599Z` / `data/backtest-artifacts/focused-july-mainline-equalscope-v2-dataset--20260408-002421` | Highest-signal conclusion: runtime provenance drift is still in play. `scripts/replay-focused.sh` validates the deployed worker, not local code, and the deployed runtime is still emitting at least one excluded weak-context reject (`tt_weak_context_rank_inflation_guard`) that should not be part of the frozen July package. This likely explains why the dominant `XLY` branch remains over-pruned even after the local candidate freeze removed those guards from source. Before treating any remaining basket sparsity as a fresh strategy problem, the next honest step is to reconcile and prove the deployed worker version against the frozen July package. | preserved |
| 2026-04-08 | july-mainline-equalscope-v3-postdeploy | replay | `worker/index.js`, `worker/pipeline/tt-core-entry.js`, `scripts/replay-focused.sh` | Deployed the current local worker package to both wrangler targets, verified on an isolated `XLY` Jul 1 replay that the excluded `tt_weak_context_rank_inflation_guard` no longer appeared at runtime, and then reran the full clean equal-scope July basket on the same frozen dataset manifest and pinned July config. | `focused_replay_20260408-073613@2026-04-08T14:36:16.059Z` / `data/backtest-artifacts/focused-july-mainline-equalscope-v3-postdeploy--20260408-073613` | Provenance mattered materially. The post-deploy lane improved from `13` to `18` run trades (`14` closed, `8W / 6L / 4 open-or-trimmed`, about `$5,019.72` realized PnL and `$5,574.67` total), and the traded-name count widened from `9` to `12`, adding names like `HUBS`, `ABT`, and `SWK`. The isolated `XLY` Jul 1 probe also changed exactly as hoped: `tt_weak_context_rank_inflation_guard` disappeared and was replaced by baseline-style blockers such as `tt_pullback_not_deep_enough`, `tt_momentum_30m_5_12_unconfirmed`, and `tt_doa_d_4h_1h_against`. July is still not broad-scope healthy versus the archived `66`-trade basket, but the stale deployed-runtime ingredient is now proven and partially removed. The remaining gap should now be investigated as true strategy/path sparsity rather than deployment drift. | preserved |
| 2026-04-08 | xly-postdeploy-route-forensics-v1 | forensics | `worker/index.js`, `worker/pipeline/tt-core-entry.js`, `configs/xly-proof-pullback-selective-off-20260408.json`, `tasks/todo.md` | Ran a narrow post-deploy `XLY` proof in two stages. First, compared the pinned July config against a one-key variant with `deep_audit_pullback_selective_enabled=false`; both full-window `focused` lanes finished with `0` `XLY` trades, proving the selective non-prime pullback rank gate is not the dominant missing-trade ingredient. Then compared isolated `replay-ticker-d1` probes against the actual `candle-replay` route on the six archived golden `XLY` dates (`2025-07-01`, `2025-07-08`, `2025-07-11`, `2025-07-17`, `2025-07-25`, `2025-08-05`). | `focused_replay_20260408-085122@2026-04-08T15:51:42.057Z` / `data/backtest-artifacts/focused-xly-proof-control-postdeploy--20260408-085122`; `focused_replay_20260408-101617@2026-04-08T17:16:20.956Z` / `data/backtest-artifacts/focused-xly-proof-pullback-selective-off--20260408-101617` | Two concrete conclusions. First, disabling `deep_audit_pullback_selective_enabled` changed nothing (`0` vs `0`), so `tt_pullback_non_prime_rank_selective` is not the primary `XLY` blocker. Second, the single-ticker debug route is misleading for this clean-lane question: `replay-ticker-d1` can still show isolated `XLY` trade creation on Jul 1 / Jul 11 / Jul 17, but the real `candle-replay` lane stays at `0` and surfaces the actual blocker mix. Across the five July golden dates, the dominant `candle-replay` blockers were `tt_pullback_not_deep_enough` (`146` hits), `tt_no_trigger` (`118`), `tt_pullback_non_prime_rank_selective` (`79`), and `tt_momentum_30m_5_12_unconfirmed` (`46`); Aug 5 was a separate `tt_bias_not_aligned` case (`79`). The next honest proof should target the true full-lane `candle-replay` blocker family, especially `tt_pullback_not_deep_enough`, not the debug endpoint. | preserved |
| 2026-04-08 | xly-pullback-depth-threshold-proof-v1 | code/replay | `worker/pipeline/tt-core-entry.js`, `worker/index.js`, `configs/xly-proof-pullback-depth-1-20260408.json`, `tasks/todo.md` | Made the `tt_pullback_not_deep_enough` guard threshold configurable via `deep_audit_pullback_min_bearish_count` while preserving the current default behavior (`2`), then discovered the first proof was incomplete because the new key was not yet in `REPLAY_DA_KEYS`, so replay never loaded it from the pinned config. Added the key to the replay config whitelist, redeployed, and reran the exact same narrow `XLY` full-window proof with that threshold relaxed to `1`. Also kept the one-day Jul 1 pinned-config control check to confirm the default path still behaves the same. | `focused_replay_20260408-142442@2026-04-08T21:24:46.502Z` / `data/backtest-artifacts/focused-xly-proof-pullback-depth-1-postdeploy--20260408-142442`; `focused_replay_20260408-144450@2026-04-08T21:44:54.579Z` / `data/backtest-artifacts/focused-xly-proof-pullback-depth-1-postdeploy-v2-wired--20260408-144450`; `focused_replay_20260408-143126@2026-04-08T21:31:28.891Z` / `data/backtest-artifacts/focused-xly-control-jul1-postdeploy-guardcheck--20260408-143126` | Important correction: the first depth-threshold proof should not be trusted because replay was not yet loading `deep_audit_pullback_min_bearish_count` from the pinned config. After wiring that key into `REPLAY_DA_KEYS` and redeploying, the corrected proof still finished with `0` `XLY` trades over the full `2025-07-01 -> 2025-08-08` window (`28` days, `2212` scored, `0` run trades). That now truly rules out the bearish-depth cutoff as the single dominant `XLY` ingredient. The next blocker is further downstream in the real `candle-replay` path, most likely the trigger-freshness family (`tt_no_trigger`) and/or the remaining momentum confirmation gates rather than the raw pullback-depth count alone. | preserved |
| 2026-04-08 | xly-continuation-trigger-proof-v1 | code/replay | `worker/index.js`, `configs/xly-proof-continuation-trigger-20260408.json`, `tasks/todo.md` | Used the user's 30m chart read (`Jul 1-3`, `Jul 9-15`, `Jul 17-23` bullish continuation windows, `Jul 29-Aug 4` bearish window) to target the next `XLY` blocker family. Added a very narrow continuation-trigger proof seam that is disabled by default and only activates when a ticker stays aligned in-corridor with conservative rank/completion/phase limits, then scoped the proof config to `XLY` only. Deployed the worker and ran the full focused `XLY` window on that variant. | `focused_replay_20260408-151612@2026-04-08T22:16:16.301Z` / `data/backtest-artifacts/focused-xly-proof-continuation-trigger-postdeploy--20260408-151612` | The chart-guided continuation proof also stayed at `0` `XLY` trades over the full `2025-07-01 -> 2025-08-08` window (`28` days, `2212` scored, `0` run trades). That rules out a simple "allow aligned continuation in corridor" relaxation, at least in this conservative ticker-scoped form. Combined with the earlier selective-rank and pullback-depth proofs, the missing `XLY` branch now appears to require a more complex combination of conditions than any single narrow guard or trigger seam tested so far. | preserved |
| 2026-04-09 | fix-riot-subrunner-stoplock-savepoint | code/replay | `worker/index.js`, `tasks/july-recovery-savepoint-2026-04-09-fix-riot-subrunner-stoplock.md`, `tasks/lessons.md` | Traced the still-bad `FIX` / `RIOT` early post-trim exits through deployed-worker logs and found that the true ratchets were not just trim-time breakeven or giveback, but a second layer of stop-tightening families (`RSI_DIVERGENCE_TRAIL`, generic `DEFEND`, TD exhaustion trails, smart-runner tighten/defend) that still treated a `50%` trim like a full runner state. Patched `worker/index.js` so first partial trims do not move `trade.sl`, sub-runner trims do not use those runner-style tighten paths, and the live proof lane now holds both proof names as `TP_HIT_TRIM` instead of forcing the old immediate stop-managed pattern. | `focused_replay_20260409-011614@2026-04-09T08:16:17.708Z` / `data/backtest-artifacts/focused-fix-riot-postdeploy-structuregate-v5--20260409-011614` | User-confirmed behavioral pass: Trade Autopsy live replay showed both `RIOT` and `FIX` still open as trimmed runners with positive PnL, which is the intended management pattern for this phase. Treat this as a save point for sub-runner stop behavior, not yet as a broad July promotion candidate. | improved |

## Next Entry Template

| YYYY-MM-DD | iter-name | code/replay/backtest | `file-a`, `file-b` | Short explanation of the exact refinement. | `run_id` / `artifact path` | Metrics plus the highest-signal behavioral change. | preserved / improved / regressed |

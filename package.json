{
  "scripts": {
    "replay-ui": "node scripts/replay-ui-server.js",
    "build:analysis": "node scripts/build-index-react.js",
    "build:rail": "node scripts/compile-right-rail.js",
    "build:frontend": "node scripts/build-frontend.js",
    "deploy": "npm run build:frontend && node scripts/embed-dashboard.js && cd worker && wrangler deploy --env='' && wrangler deploy --env production",
    "deploy:worker": "npm run build:frontend && node scripts/embed-dashboard.js && cd worker && wrangler deploy --env='' && wrangler deploy --env production",
    "deploy:frontend": "bash scripts/deploy-frontend.sh",
    "deploy:all": "npm run deploy:frontend && npm run deploy:worker"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-react": "^7.23.0",
    "@tailwindcss/cli": "^4.2.2",
    "better-sqlite3": "^11.6.0",
    "tailwindcss": "^4.2.2",
    "wrangler": "^4.61.0"
  }
}

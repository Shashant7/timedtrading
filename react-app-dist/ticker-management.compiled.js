// Generated by scripts/build-frontend.js - do not edit directly
const {
  useState,
  useEffect,
  useMemo,
  useCallback,
  useRef
} = React;
const API_BASE = "";
const TFS = ["M", "W", "D", "240", "60", "30", "15", "5"];
const TF_LABELS = {
  M: "M",
  W: "W",
  D: "D",
  "240": "4H",
  "60": "1H",
  "30": "30m",
  "15": "15m",
  "5": "5m"
};
const TV_TICKERS = new Set(["ES1!", "NQ1!", "GOLD", "SILVER", "US500", "VX1!", "GC1!", "SI1!"]);
const COMPANY_NAMES = {
  AAPL: "Apple",
  AAPU: "Apple 2x ETF",
  ADBE: "Adobe",
  AEE: "Ameren",
  AEP: "American Electric",
  AEHR: "Aehr Test",
  AGQ: "Silver 2x ETF",
  ALLY: "Ally Financial",
  ALB: "Albemarle",
  AMD: "AMD",
  AMGN: "Amgen",
  AMZN: "Amazon",
  ANET: "Arista Networks",
  APD: "Air Products",
  APLD: "Applied Digital",
  APP: "AppLovin",
  ASTS: "AST SpaceMobile",
  AVAV: "AeroVironment",
  AVGO: "Broadcom",
  AWI: "Armstrong World",
  AXP: "American Express",
  AXON: "Axon Enterprise",
  AYI: "Acuity Brands",
  B: "Barnes Group",
  BA: "Boeing",
  BABA: "Alibaba",
  BE: "Bloom Energy",
  BMNR: "Bitmine Immersion",
  BTCUSD: "Bitcoin",
  BWXT: "BWX Technologies",
  CAT: "Caterpillar",
  CCJ: "Cameco",
  CDNS: "Cadence Design",
  CLS: "Celestica",
  CMCSA: "Comcast",
  COIN: "Coinbase",
  COST: "Costco",
  CRM: "Salesforce",
  CRS: "Carpenter Tech",
  CRCL: "Circle",
  CRWV: "CrowdWave",
  CRVS: "Corvus Pharma",
  CRWD: "CrowdStrike",
  CSCO: "Cisco",
  CSX: "CSX Corp",
  D: "Dominion Energy",
  DCI: "Donaldson",
  DE: "Deere & Co",
  DUK: "Duke Energy",
  DY: "Dycom",
  ECL: "Ecolab",
  EME: "EMCOR",
  EMR: "Emerson Electric",
  ENS: "EnerSys",
  ES: "Eversource",
  ETHA: "iShares Ethereum",
  ETHT: "ProShares Ether",
  ETHUSD: "Ethereum",
  ETN: "Eaton Corp",
  ETR: "Entergy",
  EXC: "Exceleron",
  EXPE: "Expedia",
  EWBC: "East West Bancorp",
  FDX: "FedEx",
  FE: "FirstEnergy",
  FIG: "Simplify MBS",
  FIX: "Comfort Systems",
  GDX: "Gold Miners ETF",
  GDXJ: "Jr Gold Miners ETF",
  GE: "GE Aerospace",
  GEV: "GE Vernova",
  GILD: "Gilead",
  GLD: "Gold ETF",
  GLXY: "Galaxy Digital",
  GOOGL: "Alphabet",
  GRNY: "Greenery",
  GS: "Goldman Sachs",
  HD: "Home Depot",
  HIMS: "Hims & Hers",
  HII: "HII Inc",
  HL: "Hecla Mining",
  HOOD: "Robinhood",
  IAU: "iShares Gold",
  IBP: "Installed Building",
  IBRX: "ImmunityBio",
  IESC: "IES Holdings",
  INTC: "Intel",
  INTU: "Intuit",
  IONQ: "IonQ",
  IOT: "Samsara",
  IREN: "IREN",
  ITT: "ITT Inc",
  IWM: "Russell 2000 ETF",
  JCI: "Johnson Controls",
  JOBY: "Joby Aviation",
  JPM: "JPMorgan",
  KLAC: "KLA Corp",
  KO: "Coca-Cola",
  KTOS: "Kratos Defense",
  LIN: "Linde",
  LITE: "Lumentum",
  LRCX: "Lam Research",
  LOW: "Lowe's",
  MA: "Mastercard",
  MCD: "McDonald's",
  MDB: "MongoDB",
  META: "Meta",
  MLI: "Mueller Industries",
  MNST: "Monster Beverage",
  MP: "MP Materials",
  MSFT: "Microsoft",
  MSTR: "MicroStrategy",
  MTB: "M&T Bank",
  MTZ: "MasTec",
  MU: "Micron",
  NBIS: "Nebius Group",
  NEE: "NextEra Energy",
  NEU: "NewMarket",
  NFLX: "Netflix",
  NKE: "Nike",
  NXT: "Nextracker",
  NVDA: "Nvidia",
  ON: "ON Semi",
  ONDS: "Ondas Holdings",
  PANW: "Palo Alto Networks",
  PATH: "UiPath",
  PEG: "PSEG",
  PEGA: "Pegasystems",
  PH: "Parker-Hannifin",
  PI: "Impinj",
  PLTR: "Palantir",
  PNC: "PNC Financial",
  PSTG: "Pure Storage",
  PWR: "Quanta Services",
  QLYS: "Qualys",
  QQQ: "Nasdaq 100 ETF",
  RBLX: "Roblox",
  RDDT: "Reddit",
  RGLD: "Royal Gold",
  RIOT: "Riot Platforms",
  RKLB: "Rocket Lab",
  SANM: "Sanmina",
  SBET: "SharpLink Gaming",
  SBUX: "Starbucks",
  SCHW: "Charles Schwab",
  SGI: "Surgalign",
  SHOP: "Shopify",
  SHW: "Sherwin-Williams",
  SLB: "SLB",
  SLV: "Silver ETF",
  SN: "SharkNinja",
  SO: "Southern Company",
  SOFI: "SoFi",
  SOXL: "Semis 3x ETF",
  SPCX: "SPAC & New Issue ETF",
  SPGI: "S&P Global",
  SPG: "Simon Property",
  SPY: "S&P 500 ETF",
  SRE: "Sempra",
  STRL: "Sterling Infra",
  STX: "Seagate",
  STZ: "Constellation Brands",
  SWK: "Stanley Black&Decker",
  T: "AT&T",
  TFC: "Truist Financial",
  TGT: "Target",
  TJX: "TJX Companies",
  TLN: "Talen Energy",
  TMO: "Thermo Fisher",
  TNA: "Small Cap 3x ETF",
  TSLA: "Tesla",
  TSM: "TSMC",
  TT: "Trane Technologies",
  TWLO: "Twilio",
  TXN: "Texas Instruments",
  ULTA: "Ulta Beauty",
  UNH: "UnitedHealth",
  UNP: "Union Pacific",
  UPS: "UPS",
  USB: "U.S. Bancorp",
  UTHR: "United Therapeutics",
  UUUU: "Energy Fuels",
  VICI: "VICI Properties",
  VRTX: "Vertex Pharma",
  VST: "Vistra Energy",
  VZ: "Verizon",
  W: "Wayfair",
  WAL: "Western Alliance",
  WEC: "WEC Energy",
  WELL: "Welltower",
  WFC: "Wells Fargo",
  WMT: "Walmart",
  WTS: "Watts Water",
  XEL: "Xcel Energy",
  XLB: "Materials ETF",
  XLC: "Comms ETF",
  XLE: "Energy ETF",
  XLF: "Financials ETF",
  XLI: "Industrials ETF",
  XLK: "Tech ETF",
  XLP: "Staples ETF",
  XLRE: "Real Estate ETF",
  XLU: "Utilities ETF",
  XLV: "Healthcare ETF",
  XLY: "Disc ETF",
  XOM: "Exxon Mobil",
  XYZ: "Block Inc",
  "ES1!": "E-mini S&P",
  "NQ1!": "E-mini Nasdaq",
  "GC1!": "Gold Futures",
  "SI1!": "Silver Futures",
  GOLD: "Gold Futures",
  SILVER: "Silver Futures",
  VIX: "VIX Index",
  "US500": "US 500",
  GLD: "Gold ETF",
  ADBE: "Adobe",
  LIN: "Linde",
  APD: "Air Products",
  FDX: "FedEx",
  CMCSA: "Comcast",
  CRM: "Salesforce",
  ECL: "Ecolab",
  SHW: "Sherwin-Williams"
};
const CONTEXT_FALLBACKS = {
  AA: {
    name: "Alcoa",
    industry: "Aluminum"
  },
  ABT: {
    name: "Abbott Laboratories",
    industry: "Medical Devices"
  },
  ACN: {
    name: "Accenture",
    industry: "IT Consulting"
  },
  AGYS: {
    name: "Agilysys",
    industry: "Software"
  },
  ARM: {
    name: "Arm Holdings",
    industry: "Semiconductors"
  },
  "BRK-B": {
    name: "Berkshire Hathaway",
    industry: "Insurance - Diversified"
  },
  CELH: {
    name: "Celsius Holdings",
    industry: "Beverages - Non-Alcoholic"
  },
  CVNA: {
    name: "Carvana",
    industry: "Auto & Truck Dealerships"
  },
  DKNG: {
    name: "DraftKings",
    industry: "Gambling"
  },
  FBL: {
    name: "GraniteShares 2x Long META Daily ETF",
    industry: "Leveraged ETF"
  },
  HUBS: {
    name: "HubSpot",
    industry: "Software"
  },
  JD: {
    name: "JD.com",
    industry: "Internet Retail"
  },
  KWEB: {
    name: "KraneShares CSI China Internet ETF",
    industry: "China Internet ETF"
  },
  LLY: {
    name: "Eli Lilly",
    industry: "Drug Manufacturers - General"
  },
  LMND: {
    name: "Lemonade",
    industry: "Insurance"
  },
  LULU: {
    name: "Lululemon",
    industry: "Apparel Retail"
  },
  RTX: {
    name: "RTX",
    industry: "Aerospace & Defense"
  },
  SNDK: {
    name: "Sandisk",
    industry: "Computer Hardware"
  },
  SPOT: {
    name: "Spotify",
    industry: "Internet Content & Information"
  },
  TEM: {
    name: "Tempus AI",
    industry: "Health Information Services"
  },
  U: {
    name: "Unity Software",
    industry: "Software"
  },
  WDC: {
    name: "Western Digital",
    industry: "Computer Hardware"
  },
  WM: {
    name: "Waste Management",
    industry: "Waste Management"
  },
  XHB: {
    name: "SPDR S&P Homebuilders ETF",
    industry: "Homebuilders ETF"
  },
  "RTY1!": {
    name: "E-mini Russell 2000 Futures",
    industry: "Equity Index Futures"
  },
  "YM1!": {
    name: "E-mini Dow Futures",
    industry: "Equity Index Futures"
  }
};
const GROUPS = {
  UPTICKS: new Set(["RDDT", "AMZN", "BABA", "TSLA", "KO", "WMT", "ETHA", "BRK-B", "MTB", "AMGN", "GILD", "CSX", "GEV", "HII", "JCI", "PH", "PWR", "TT", "CLS", "FSLR", "PANW", "CRS", "VST", "BG", "MRK", "QXO", "AXP"]),
  GRNI: new Set(),
  GRNJ: new Set(),
  GRNY: new Set(),
  SP_Sectors: new Set(["XLK", "XLF", "XLY", "XLP", "XLC", "XLI", "XLB", "XLE", "XLRE", "XLU", "XLV"]),
  Futures: new Set(["ES1!", "NQ1!", "RTY1!", "YM1!", "GC1!", "SI1!", "CL1!", "BTCUSD", "ETHUSD", "VX1!"])
};
let _etfWeightMap = {};
const GROUP_ORDER = ["SP_Sectors", "Futures", "UPTICKS", "GRNI", "GRNJ", "GRNY"];
async function loadETFGroups() {
  try {
    const [etfRes, upticksRes] = await Promise.all([fetch("/timed/etf/groups"), fetch(`${API_BASE}/timed/admin/upticks`, {
      credentials: "include"
    })]);
    const data = await etfRes.json();
    if (data.ok && data.groups) {
      for (const [etf, tickers] of Object.entries(data.groups)) {
        if (GROUPS[etf]) {
          GROUPS[etf] = new Set(tickers.map(t => String(t).toUpperCase()));
        }
      }
    }
    const upticksData = await upticksRes.json();
    if (upticksData.ok && Array.isArray(upticksData.tickers)) {
      GROUPS.UPTICKS = new Set(upticksData.tickers.map(t => String(t).toUpperCase()));
    }
    const holdingResults = await Promise.allSettled(["GRNY", "GRNJ", "GRNI"].map(async etf => {
      const r = await fetch(`/timed/etf/holdings/${etf}`);
      const d = await r.json();
      return {
        etf,
        data: d
      };
    }));
    for (const result of holdingResults) {
      if (result.status !== "fulfilled") continue;
      const {
        etf,
        data: d
      } = result.value;
      if (!d?.ok || !Array.isArray(d.holdings)) continue;
      for (const h of d.holdings) {
        if (!_etfWeightMap[h.ticker]) _etfWeightMap[h.ticker] = {};
        _etfWeightMap[h.ticker][etf] = h.weight;
      }
    }
  } catch (e) {
    console.warn("[ETF GROUPS] Failed to load dynamic groups:", e);
  }
}
function groupsForTicker(t) {
  const T = String(t || "").toUpperCase();
  const out = [];
  for (const [g, set] of Object.entries(GROUPS)) {
    if (set.has(T)) out.push(g);
  }
  return out;
}
function etfWeightBadge(ticker, group) {
  const w = _etfWeightMap[String(ticker).toUpperCase()]?.[group];
  return w ? `${w.toFixed(1)}%` : "";
}
function pctColor(pct) {
  if (pct >= 80) return "#22c55e";
  if (pct >= 40) return "#f59e0b";
  return "#ef4444";
}
function pctBg(pct) {
  if (pct >= 80) return "rgba(34,197,94,0.12)";
  if (pct >= 40) return "rgba(245,158,11,0.12)";
  return "rgba(239,68,68,0.12)";
}
function formatAge(ts) {
  if (!ts) return "—";
  const now = Date.now();
  const ageMs = now - ts;
  if (ageMs < 0) return "now";
  const mins = Math.floor(ageMs / 60000);
  if (mins < 60) return `${mins}m ago`;
  const hrs = Math.floor(mins / 60);
  if (hrs < 24) return `${hrs}h ago`;
  const days = Math.floor(hrs / 24);
  if (days < 7) return `${days}d ago`;
  return new Date(ts).toLocaleDateString("en-US", {
    month: "short",
    day: "numeric"
  });
}
function formatDate(ts) {
  if (!ts) return "—";
  return new Date(ts).toLocaleDateString("en-US", {
    month: "short",
    day: "numeric",
    year: "2-digit"
  });
}
const GICS_SECTORS = ["Information Technology", "Health Care", "Financials", "Industrials", "Consumer Discretionary", "Consumer Staples", "Communication Services", "Energy", "Basic Materials", "Real Estate", "Utilities", "Index ETF", "Sector ETF", "Thematic ETF", "Commodity ETF", "Crypto", "Precious Metals", "Unknown"];
function CoreUniverseManager() {
  const [state, setState] = useState({
    loading: true,
    data: null,
    error: null
  });
  const [tickerInput, setTickerInput] = useState("");
  const [sectorInput, setSectorInput] = useState("Information Technology");
  const [submitting, setSubmitting] = useState(false);
  const [msg, setMsg] = useState(null);
  const [open, setOpen] = useState(true);
  const load = async () => {
    setState(s => ({
      ...s,
      loading: true,
      error: null
    }));
    try {
      const r = await fetch(`${API_BASE}/timed/admin/universe`, {
        credentials: "include",
        cache: "no-store"
      });
      const d = await r.json();
      if (d.ok) {
        setState({
          loading: false,
          data: d,
          error: null
        });
      } else {
        setState({
          loading: false,
          data: null,
          error: d.error || "load_failed"
        });
      }
    } catch (e) {
      setState({
        loading: false,
        data: null,
        error: e.message
      });
    }
  };
  useEffect(() => {
    load();
  }, []);
  const add = async () => {
    const t = tickerInput.toUpperCase().trim();
    if (!t) return;
    if (!/^[A-Z]{1,5}(-[A-Z]{1,2})?$/.test(t)) {
      setMsg({
        type: "error",
        text: `Invalid ticker format: ${t}`
      });
      return;
    }
    setSubmitting(true);
    setMsg(null);
    try {
      const r = await fetch(`${API_BASE}/timed/admin/universe`, {
        method: "POST",
        credentials: "include",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({
          ticker: t,
          sector: sectorInput
        })
      });
      const d = await r.json();
      if (d.ok) {
        setMsg({
          type: "success",
          text: d.already_in_core ? `${t} is already in the core universe.` : `${t} added to the universe — backfill + scoring starting (~2-5 min).`
        });
        setTickerInput("");
        load();
      } else {
        setMsg({
          type: "error",
          text: `${d.error || "add_failed"}${d.detail ? ` — ${d.detail}` : ""}`
        });
      }
    } catch (e) {
      setMsg({
        type: "error",
        text: e.message
      });
    } finally {
      setSubmitting(false);
    }
  };
  const remove = async ticker => {
    if (!ticker) return;
    if (!confirm(`Remove ${ticker} from the universe? It will be hidden from all users.`)) return;
    setSubmitting(true);
    setMsg(null);
    try {
      const r = await fetch(`${API_BASE}/timed/admin/universe/${encodeURIComponent(ticker)}`, {
        method: "DELETE",
        credentials: "include"
      });
      const d = await r.json();
      if (d.ok) {
        setMsg({
          type: "success",
          text: d.was_canonical ? `${ticker} added to removal blocklist (hardcoded — will be hidden on next isolate restart).` : `${ticker} removed from the universe.`
        });
        load();
      } else {
        setMsg({
          type: "error",
          text: `${d.error || "remove_failed"}${d.detail ? ` — ${d.detail}` : ""}`
        });
      }
    } catch (e) {
      setMsg({
        type: "error",
        text: e.message
      });
    } finally {
      setSubmitting(false);
    }
  };
  const overlay = state.data?.overlay || [];
  const removed = state.data?.removed || [];
  const totalActive = state.data?.total_active ?? "—";
  const coreCount = state.data?.core?.count ?? "—";
  return React.createElement("div", {
    className: "tt-admin-section"
  }, React.createElement("button", {
    onClick: () => setOpen(v => !v),
    className: "tt-admin-head group"
  }, React.createElement("span", {
    className: "tt-admin-title",
    style: {
      color: "var(--tt-accent)"
    }
  }, "Core Universe"), React.createElement("span", {
    className: "text-[10px] px-2 py-0.5 rounded-full",
    style: {
      background: "var(--tt-accent-dim)",
      color: "var(--tt-accent)",
      fontFamily: "var(--tt-font-mono)"
    }
  }, totalActive), React.createElement("span", {
    className: "text-[10px]",
    style: {
      color: "var(--tt-text-faint)"
    }
  }, "core ", coreCount, " \xB7 overlay ", overlay.length, " \xB7 removed ", removed.length), React.createElement("span", {
    className: "text-xs ml-auto",
    style: {
      color: "var(--tt-text-dim)"
    }
  }, open ? "▲" : "▼")), open && React.createElement("div", {
    className: "mt-3 space-y-3"
  }, React.createElement("div", {
    className: "tt-page-lede",
    style: {
      fontSize: 12
    }
  }, "Add tickers here to update the live universe for all users. Validates against the market data feed, then runs backfill + scoring (~2\u20135 min). Use Fill / Score on empty rows below if coverage is stale."), React.createElement("div", {
    className: "flex items-center gap-2 flex-wrap"
  }, React.createElement("input", {
    value: tickerInput,
    onChange: e => setTickerInput(e.target.value),
    onKeyDown: e => e.key === "Enter" && add(),
    placeholder: "Ticker (e.g. IBM)",
    className: "tt-input w-40 uppercase",
    style: {
      fontFamily: "var(--tt-font-mono)"
    }
  }), React.createElement("select", {
    value: sectorInput,
    onChange: e => setSectorInput(e.target.value),
    className: "tt-input"
  }, GICS_SECTORS.map(s => React.createElement("option", {
    key: s,
    value: s
  }, s))), React.createElement("button", {
    onClick: add,
    disabled: submitting || !tickerInput.trim(),
    className: "tt-btn tt-btn--primary"
  }, submitting ? React.createElement(React.Fragment, null, React.createElement("span", {
    className: "tt-spin"
  }), "Adding\u2026") : "Add to Universe")), msg && React.createElement("div", {
    className: `text-xs px-2 py-1 rounded ${msg.type === "success" ? "text-green-400 bg-green-900/20" : "text-red-400 bg-red-900/20"}`
  }, msg.text), overlay.length > 0 && React.createElement("div", null, React.createElement("div", {
    className: "text-[10px] text-gray-500 uppercase tracking-wide mb-1.5"
  }, "Overlay (KV-added, not in core)"), React.createElement("div", {
    className: "flex flex-wrap gap-1.5"
  }, overlay.map(o => React.createElement("span", {
    key: o.ticker,
    className: "inline-flex items-center gap-1 px-2 py-1 rounded-md bg-cyan-500/10 border border-cyan-500/20 text-[11px] text-cyan-300 font-medium"
  }, o.ticker, React.createElement("span", {
    className: "text-cyan-500/60 text-[9px]"
  }, "\xB7 ", o.sector || "Unknown"), React.createElement("button", {
    onClick: () => remove(o.ticker),
    className: "text-cyan-500/60 hover:text-red-400 ml-0.5 text-[10px]",
    title: "Remove from universe"
  }, "\u2715"))))), removed.length > 0 && React.createElement("div", null, React.createElement("div", {
    className: "text-[10px] text-gray-500 uppercase tracking-wide mb-1.5"
  }, "Removed (blocklist \u2014 won't be re-added)"), React.createElement("div", {
    className: "flex flex-wrap gap-1.5"
  }, removed.map(t => React.createElement("span", {
    key: t,
    className: "inline-flex items-center gap-1 px-2 py-1 rounded-md bg-red-500/5 border border-red-500/15 text-[11px] text-red-400/70 font-medium"
  }, t)))), state.loading && React.createElement("div", {
    className: "text-[10px] text-gray-600"
  }, "Loading\u2026"), state.error && React.createElement("div", {
    className: "text-[10px] text-red-400"
  }, "Error: ", state.error)));
}
function UptickManager() {
  const [tickers, setTickers] = useState([...GROUPS.UPTICKS]);
  const [input, setInput] = useState("");
  const [saving, setSaving] = useState(false);
  const [msg, setMsg] = useState(null);
  const [open, setOpen] = useState(false);
  useEffect(() => {
    fetch(`${API_BASE}/timed/admin/upticks`, {
      credentials: "include"
    }).then(r => r.json()).then(d => {
      if (d.ok && Array.isArray(d.tickers)) {
        setTickers(d.tickers);
        GROUPS.UPTICKS = new Set(d.tickers.map(t => String(t).toUpperCase()));
      }
    }).catch(() => {});
  }, []);
  const addTickers = () => {
    const raw = input.toUpperCase().split(/[,\s]+/).filter(Boolean);
    if (!raw.length) return;
    setTickers(prev => [...new Set([...prev, ...raw])]);
    setInput("");
  };
  const removeTicker = t => setTickers(prev => prev.filter(x => x !== t));
  const save = async () => {
    setSaving(true);
    setMsg(null);
    try {
      const r = await fetch(`${API_BASE}/timed/admin/upticks`, {
        method: "PUT",
        headers: {
          "Content-Type": "application/json"
        },
        credentials: "include",
        body: JSON.stringify({
          tickers
        })
      });
      const d = await r.json();
      if (d.ok) {
        setMsg({
          type: "success",
          text: `Saved ${d.count} tickers`
        });
        GROUPS.UPTICKS = new Set(tickers);
      } else {
        setMsg({
          type: "error",
          text: d.error || "Save failed"
        });
      }
    } catch (e) {
      setMsg({
        type: "error",
        text: e.message
      });
    } finally {
      setSaving(false);
    }
  };
  return React.createElement("div", {
    className: "tt-admin-section"
  }, React.createElement("button", {
    onClick: () => setOpen(v => !v),
    className: "tt-admin-head group"
  }, React.createElement("span", {
    className: "tt-admin-title",
    style: {
      color: "#4ade80"
    }
  }, "TT Selected \u2014 Upticks"), React.createElement("span", {
    className: "text-[10px] text-gray-500 bg-white/[0.04] px-1.5 py-0.5 rounded"
  }, tickers.length), React.createElement("span", {
    className: "text-gray-600 text-xs group-hover:text-white ml-auto"
  }, open ? "▲" : "▼")), open && React.createElement("div", {
    className: "mt-3 space-y-3"
  }, React.createElement("div", {
    className: "flex flex-wrap gap-1.5"
  }, tickers.sort().map(t => React.createElement("span", {
    key: t,
    className: "inline-flex items-center gap-1 px-2 py-1 rounded-md bg-emerald-500/10 border border-emerald-500/20 text-[11px] text-emerald-300 font-medium"
  }, t, React.createElement("button", {
    onClick: () => removeTicker(t),
    className: "text-emerald-500/60 hover:text-red-400 ml-0.5 text-[10px]"
  }, "\u2715")))), React.createElement("div", {
    className: "flex items-center gap-2"
  }, React.createElement("input", {
    value: input,
    onChange: e => setInput(e.target.value),
    onKeyDown: e => e.key === "Enter" && addTickers(),
    placeholder: "Add tickers: AAPL, MSFT...",
    className: "bg-white/5 border border-white/10 rounded px-3 py-1.5 text-sm text-white placeholder-gray-600 w-64 focus:outline-none focus:border-emerald-500/50"
  }), React.createElement("button", {
    onClick: addTickers,
    disabled: !input.trim(),
    className: "px-3 py-1.5 bg-emerald-600/60 hover:bg-emerald-600 rounded text-xs font-medium text-white disabled:opacity-40"
  }, "Add"), React.createElement("button", {
    onClick: save,
    disabled: saving,
    className: "px-3 py-1.5 bg-blue-600/60 hover:bg-blue-600 rounded text-xs font-medium text-white disabled:opacity-40 ml-auto"
  }, saving ? "Saving..." : "Save Changes")), msg && React.createElement("div", {
    className: `text-xs px-2 py-1 rounded ${msg.type === "success" ? "text-green-400 bg-green-900/20" : "text-red-400 bg-red-900/20"}`
  }, msg.text)));
}
function MemberTickerListManager() {
  const [tickers, setTickers] = useState([]);
  const [savedTickers, setSavedTickers] = useState([]);
  const [input, setInput] = useState("");
  const [saving, setSaving] = useState(false);
  const [msg, setMsg] = useState(null);
  const [open, setOpen] = useState(false);
  useEffect(() => {
    fetch(`${API_BASE}/timed/member-tickers`, {
      credentials: "include"
    }).then(r => r.json()).then(d => {
      if (d.ok && Array.isArray(d.tickers)) {
        setTickers(d.tickers);
        setSavedTickers(d.tickers);
      }
    }).catch(() => {});
  }, []);
  const addTickers = () => {
    const raw = input.toUpperCase().split(/[,\s]+/).filter(Boolean);
    if (!raw.length) return;
    setTickers(prev => [...new Set([...prev, ...raw])]);
    setInput("");
  };
  const removeTicker = t => setTickers(prev => prev.filter(x => x !== t));
  const hasUnsavedChanges = JSON.stringify([...tickers].sort()) !== JSON.stringify([...savedTickers].sort());
  const save = async () => {
    setSaving(true);
    setMsg(null);
    try {
      const r = await fetch(`${API_BASE}/timed/admin/member-tickers`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        credentials: "include",
        body: JSON.stringify({
          tickers
        })
      });
      let d;
      try {
        d = await r.json();
      } catch {
        d = {};
      }
      if (d.ok) {
        setSavedTickers([...tickers]);
        setMsg({
          type: "success",
          text: `Saved ${d.count} tickers for Member users`
        });
      } else {
        const errText = r.status === 401 || r.status === 403 ? "Not authorized. Sign in as admin or use API key." : d.error || "Save failed";
        setMsg({
          type: "error",
          text: errText
        });
      }
    } catch (e) {
      setMsg({
        type: "error",
        text: e.message
      });
    } finally {
      setSaving(false);
    }
  };
  return React.createElement("div", {
    className: "tt-admin-section"
  }, React.createElement("button", {
    onClick: () => setOpen(v => !v),
    className: "tt-admin-head group"
  }, React.createElement("span", {
    className: "tt-admin-title",
    style: {
      color: "#fbbf24"
    }
  }, "Member Ticker List"), React.createElement("span", {
    className: "text-[10px] text-gray-500 bg-white/[0.04] px-1.5 py-0.5 rounded"
  }, tickers.length), hasUnsavedChanges && React.createElement("span", {
    className: "text-[9px] text-amber-400 font-medium"
  }, "(unsaved)"), React.createElement("span", {
    className: "text-gray-600 text-xs group-hover:text-white ml-auto"
  }, open ? "▲" : "▼")), open && React.createElement("div", {
    className: "mt-3 space-y-3"
  }, React.createElement("div", {
    className: "text-[10px] text-[#6b7280]"
  }, "Tickers visible to free Members in Investor Mode. Pro/Admin users see all tickers."), React.createElement("div", {
    className: "flex flex-wrap gap-1.5"
  }, tickers.sort().map(t => React.createElement("span", {
    key: t,
    className: "inline-flex items-center gap-1 px-2 py-1 rounded-md bg-amber-500/10 border border-amber-500/20 text-[11px] text-amber-300 font-medium"
  }, t, React.createElement("button", {
    onClick: () => removeTicker(t),
    className: "text-amber-500/60 hover:text-red-400 ml-0.5 text-[10px]"
  }, "\u2715")))), React.createElement("div", {
    className: "flex items-center gap-2"
  }, React.createElement("input", {
    value: input,
    onChange: e => setInput(e.target.value),
    onKeyDown: e => e.key === "Enter" && addTickers(),
    placeholder: "Add tickers: AAPL, MSFT...",
    className: "bg-white/5 border border-white/10 rounded px-3 py-1.5 text-sm text-white placeholder-gray-600 w-64 focus:outline-none focus:border-amber-500/50"
  }), React.createElement("button", {
    onClick: addTickers,
    disabled: !input.trim(),
    className: "px-3 py-1.5 bg-amber-600/60 hover:bg-amber-600 rounded text-xs font-medium text-white disabled:opacity-40"
  }, "Add"), React.createElement("button", {
    onClick: save,
    disabled: saving || !hasUnsavedChanges,
    className: "px-3 py-1.5 bg-blue-600/60 hover:bg-blue-600 rounded text-xs font-medium text-white disabled:opacity-40 ml-auto",
    title: !hasUnsavedChanges ? "No changes to save" : "Save Member ticker list"
  }, saving ? "Saving..." : hasUnsavedChanges ? "Save Changes" : "Saved")), msg && React.createElement("div", {
    className: `text-xs px-2 py-1 rounded ${msg.type === "success" ? "text-green-400 bg-green-900/20" : "text-red-400 bg-red-900/20"}`
  }, msg.text)));
}
const RATING_OPTIONS = ["overweight", "neutral", "underweight"];
const RATING_COLORS = {
  overweight: "text-green-400",
  neutral: "text-gray-400",
  underweight: "text-red-400"
};
const RATING_BG = {
  overweight: "bg-green-900/20 border-green-800/30",
  neutral: "bg-white/[0.03] border-white/[0.06]",
  underweight: "bg-red-900/20 border-red-800/30"
};
function SectorRatingsManager() {
  const [ratings, setRatings] = useState({});
  const [saving, setSaving] = useState(false);
  const [msg, setMsg] = useState(null);
  const [open, setOpen] = useState(false);
  useEffect(() => {
    fetch(`${API_BASE}/timed/admin/sector-ratings`, {
      credentials: "include"
    }).then(r => r.json()).then(d => {
      if (d.ok && d.ratings) setRatings(d.ratings);
    }).catch(() => {});
  }, []);
  const updateField = (sector, field, value) => {
    setRatings(prev => ({
      ...prev,
      [sector]: {
        ...prev[sector],
        [field]: field === "rating" ? value : Number(value) || 0
      }
    }));
  };
  const save = async () => {
    setSaving(true);
    setMsg(null);
    try {
      const r = await fetch(`${API_BASE}/timed/admin/sector-ratings`, {
        method: "PUT",
        headers: {
          "Content-Type": "application/json"
        },
        credentials: "include",
        body: JSON.stringify({
          ratings
        })
      });
      const d = await r.json();
      if (d.ok) setMsg({
        type: "success",
        text: `Updated ${d.updated} sectors`
      });else setMsg({
        type: "error",
        text: d.error || "Save failed"
      });
    } catch (e) {
      setMsg({
        type: "error",
        text: e.message
      });
    } finally {
      setSaving(false);
    }
  };
  const sectors = Object.keys(ratings).filter(s => !["ETF", "Crypto", "Precious Metals"].includes(s));
  return React.createElement("div", {
    className: "tt-admin-section"
  }, React.createElement("button", {
    onClick: () => setOpen(v => !v),
    className: "tt-admin-head group"
  }, React.createElement("span", {
    className: "tt-admin-title",
    style: {
      color: "#60a5fa"
    }
  }, "S&P Sector Ratings"), React.createElement("span", {
    className: "text-[10px] text-gray-500 bg-white/[0.04] px-1.5 py-0.5 rounded"
  }, sectors.length, " sectors"), React.createElement("span", {
    className: "text-gray-600 text-xs group-hover:text-white ml-auto"
  }, open ? "▲" : "▼")), open && React.createElement("div", {
    className: "mt-3 space-y-2"
  }, React.createElement("div", {
    className: "overflow-x-auto"
  }, React.createElement("table", {
    className: "text-sm w-full",
    style: {
      maxWidth: 900
    }
  }, React.createElement("thead", null, React.createElement("tr", {
    className: "text-[10px] text-gray-500 uppercase border-b border-white/[0.06]"
  }, React.createElement("th", {
    className: "text-left py-1.5 px-2 w-[180px]"
  }, "Sector"), React.createElement("th", {
    className: "text-center py-1.5 px-2 w-[130px]"
  }, "Rating"), React.createElement("th", {
    className: "text-center py-1.5 px-2 w-[80px]"
  }, "Boost"), React.createElement("th", {
    className: "text-center py-1.5 px-2 w-[90px]"
  }, "S&P Wt%"), React.createElement("th", {
    className: "text-center py-1.5 px-2 w-[90px]"
  }, "FSI Wt%"), React.createElement("th", {
    className: "text-center py-1.5 px-2 w-[80px]"
  }, "Delta"))), React.createElement("tbody", null, sectors.sort((a, b) => (ratings[b]?.delta || 0) - (ratings[a]?.delta || 0)).map(sector => {
    const r = ratings[sector] || {};
    const ratingCls = RATING_BG[r.rating] || RATING_BG.neutral;
    return React.createElement("tr", {
      key: sector,
      className: `border-b border-white/[0.03] ${ratingCls}`
    }, React.createElement("td", {
      className: "py-1.5 px-2 text-[12px] text-white font-medium"
    }, sector), React.createElement("td", {
      className: "py-1 px-2 text-center"
    }, React.createElement("select", {
      value: r.rating || "neutral",
      onChange: e => updateField(sector, "rating", e.target.value),
      className: `bg-transparent border border-white/10 rounded px-2 py-0.5 text-[11px] font-semibold focus:outline-none ${RATING_COLORS[r.rating] || "text-gray-400"}`,
      style: {
        background: "rgba(10,10,15,0.8)"
      }
    }, RATING_OPTIONS.map(o => React.createElement("option", {
      key: o,
      value: o,
      style: {
        background: "#1a1a24"
      }
    }, o.charAt(0).toUpperCase() + o.slice(1))))), React.createElement("td", {
      className: "py-1 px-2 text-center"
    }, React.createElement("input", {
      type: "number",
      value: r.boost ?? 0,
      onChange: e => updateField(sector, "boost", e.target.value),
      className: "bg-white/5 border border-white/10 rounded px-2 py-0.5 text-[11px] text-white w-16 text-center focus:outline-none focus:border-blue-500/50",
      step: 1
    })), React.createElement("td", {
      className: "py-1 px-2 text-center"
    }, React.createElement("input", {
      type: "number",
      value: r.spWeight ?? 0,
      onChange: e => updateField(sector, "spWeight", e.target.value),
      className: "bg-white/5 border border-white/10 rounded px-2 py-0.5 text-[11px] text-white w-16 text-center focus:outline-none",
      step: 0.1
    })), React.createElement("td", {
      className: "py-1 px-2 text-center"
    }, React.createElement("input", {
      type: "number",
      value: r.fsiWeight ?? 0,
      onChange: e => updateField(sector, "fsiWeight", e.target.value),
      className: "bg-white/5 border border-white/10 rounded px-2 py-0.5 text-[11px] text-white w-16 text-center focus:outline-none",
      step: 0.1
    })), React.createElement("td", {
      className: "py-1 px-2 text-center"
    }, React.createElement("span", {
      className: `text-[11px] font-semibold tabular-nums ${(r.delta || 0) > 0 ? "text-green-400" : (r.delta || 0) < 0 ? "text-red-400" : "text-gray-500"}`
    }, Number.isFinite(r.delta) ? (r.delta > 0 ? "+" : "") + r.delta.toFixed(1) + "%" : "—")));
  })))), React.createElement("div", {
    className: "flex items-center gap-2"
  }, React.createElement("button", {
    onClick: save,
    disabled: saving,
    className: "px-3 py-1.5 bg-blue-600/60 hover:bg-blue-600 rounded text-xs font-medium text-white disabled:opacity-40"
  }, saving ? "Saving..." : "Save Ratings"), msg && React.createElement("span", {
    className: `text-xs ${msg.type === "success" ? "text-green-400" : "text-red-400"}`
  }, msg.text))));
}
function formatBfLabel(status) {
  if (!status) return "Fill";
  if (status === "running" || status === "started") return "Filling…";
  if (status === "scoring...") return "Scoring…";
  if (status === "done") return "Done";
  if (typeof status === "string" && status.startsWith("error")) return "Retry";
  if (typeof status === "string" && (status.includes("bars") || status.includes("TF"))) {
    return status.length > 18 ? status.slice(0, 18) + "…" : status;
  }
  return String(status).slice(0, 14);
}
function formatScoreLabel(status) {
  if (!status) return "Score";
  if (status === "running") return "Scoring…";
  if (status === "ok") return "Scored";
  if (typeof status === "string" && status.startsWith("error")) return "Retry";
  return "Score";
}
function App() {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);
  const [search, setSearch] = useState("");
  const [sectorFilter, setSectorFilter] = useState("all");
  const [sortCol, setSortCol] = useState("ticker");
  const [sortDir, setSortDir] = useState("asc");
  const [selected, setSelected] = useState(new Set());
  const [actionStatus, setActionStatus] = useState(null);
  const [backfillStatus, setBackfillStatus] = useState({});
  const [priceData, setPriceData] = useState({});
  const [contextData, setContextData] = useState({});
  const [pendingTickers, setPendingTickers] = useState(new Set());
  const [watchlistLoaded, setWatchlistLoaded] = useState(false);
  const pollUntilRef = useRef(null);
  const [watchlistTickers, setWatchlistTickers] = useState([]);
  const normalizeTicker = useCallback(ticker => String(ticker || "").toUpperCase().trim(), []);
  const getTickerName = useCallback(ticker => {
    const ctx = contextData[ticker];
    if (ctx?.name) return ctx.name;
    if (CONTEXT_FALLBACKS[ticker]?.name) return CONTEXT_FALLBACKS[ticker].name;
    return COMPANY_NAMES[ticker] || "—";
  }, [contextData]);
  const getTickerIndustry = useCallback(ticker => {
    const ctx = contextData[ticker];
    return ctx?.industry || CONTEXT_FALLBACKS[ticker]?.industry || "";
  }, [contextData]);
  const getTickerMCap = useCallback(ticker => {
    const ctx = contextData[ticker];
    const v = ctx?.market_cap ? Number(ctx.market_cap) : null;
    if (!v || !Number.isFinite(v) || v <= 0) return null;
    return v;
  }, [contextData]);
  const fmtMCap = v => {
    if (!v) return "—";
    if (v >= 1e12) return `$${(v / 1e12).toFixed(1)}T`;
    if (v >= 1e9) return `$${(v / 1e9).toFixed(1)}B`;
    if (v >= 1e6) return `$${(v / 1e6).toFixed(0)}M`;
    return `$${v.toLocaleString()}`;
  };
  const fetchData = useCallback(async (clearPendingFromResponse = true, skipLoading = false) => {
    if (!skipLoading) setLoading(true);
    setError(null);
    let loadingCleared = false;
    try {
      const pricePromise = fetch(`${API_BASE}/timed/prices?_t=${Date.now()}`).then(r => r.json()).then(pJson => {
        if (pJson?.ok && pJson.prices) {
          setPriceData({
            prices: pJson.prices,
            updated_at: pJson.updated_at
          });
        }
      }).catch(() => {});
      const requestTs = Date.now();
      const watchlistPromise = fetch(`${API_BASE}/timed/tickers?_t=${requestTs}`).then(r => r.json()).then(tJson => {
        if (tJson.ok && Array.isArray(tJson.tickers)) {
          setWatchlistTickers(tJson.tickers.map(normalizeTicker).filter(Boolean));
          setWatchlistLoaded(true);
        }
      }).catch(() => {});
      const statusRes = await fetch(`${API_BASE}/timed/admin/ingestion-status?_t=${requestTs}`, {
        credentials: "include"
      });
      const json = await statusRes.json();
      if (!json.ok) throw new Error(json.error || "Failed to fetch");
      setData(json);
      if (Array.isArray(json.tickers)) {
        const ctx = {};
        json.tickers.forEach(row => {
          if (row?.ticker && row?.context && typeof row.context === "object") {
            ctx[normalizeTicker(row.ticker)] = row.context;
          }
        });
        try {
          const metaRes = await fetch(`${API_BASE}/timed/admin/ticker-metadata/all?_t=${requestTs}`, {
            credentials: "include",
            cache: "no-store"
          });
          if (metaRes.ok) {
            const metaJson = await metaRes.json();
            if (metaJson?.ok && metaJson?.by_ticker) {
              for (const [sym, meta] of Object.entries(metaJson.by_ticker)) {
                const t = normalizeTicker(sym);
                const merged = {
                  ...(ctx[t] || {})
                };
                if (!merged.name && meta.name) merged.name = meta.name;
                if (!merged.sector && meta.sector) merged.sector = meta.sector;
                if (!merged.industry && meta.industry) merged.industry = meta.industry;
                if (!merged.market_cap && meta.market_cap) merged.market_cap = meta.market_cap;
                ctx[t] = merged;
              }
            }
          }
        } catch (_) {}
        setContextData(ctx);
      }
      if (clearPendingFromResponse && json.tickers && json.tickers.length) {
        setPendingTickers(prev => {
          const next = new Set(prev);
          json.tickers.forEach(t => next.delete(normalizeTicker(t.ticker)));
          return next;
        });
      }
      if (!skipLoading) {
        setLoading(false);
        loadingCleared = true;
      }
      void Promise.allSettled([pricePromise, watchlistPromise]);
    } catch (e) {
      setError(e.message);
    } finally {
      if (!skipLoading && !loadingCleared) setLoading(false);
    }
  }, [normalizeTicker]);
  useEffect(() => {
    loadETFGroups();
    fetchData();
  }, [fetchData]);
  useEffect(() => {
    const interval = setInterval(() => {
      fetch(`${API_BASE}/timed/prices?_t=${Date.now()}`).then(r => r.json()).then(pJson => {
        if (pJson?.ok && pJson.prices) {
          setPriceData({
            prices: pJson.prices,
            updated_at: pJson.updated_at
          });
        }
      }).catch(() => {});
    }, 45_000);
    return () => clearInterval(interval);
  }, []);
  useEffect(() => {
    if (pendingTickers.size === 0 || !pollUntilRef.current) return;
    const interval = setInterval(() => {
      if (Date.now() > pollUntilRef.current) {
        pollUntilRef.current = null;
        return;
      }
      fetchData(true, true);
    }, 4000);
    return () => clearInterval(interval);
  }, [pendingTickers.size, fetchData]);
  const tickers = useMemo(() => {
    const fromApi = data?.tickers || [];
    const activeUniverse = new Set([...(watchlistTickers || []).map(normalizeTicker).filter(Boolean), ...Array.from(pendingTickers).map(normalizeTicker).filter(Boolean)]);
    const filteredFromApi = watchlistLoaded ? fromApi.filter(t => activeUniverse.has(normalizeTicker(t.ticker))) : fromApi;
    const enrichedFromApi = filteredFromApi.map(t => {
      const hasData = Object.values(t.tfs || {}).some(tf => tf && tf.count > 0);
      if (t.needs_onboard || !hasData && (t.pct === 0 || t.quality === 0)) {
        return {
          ...t,
          needs_onboard: true
        };
      }
      return t;
    });
    const fromApiSet = new Set(enrichedFromApi.map(t => normalizeTicker(t.ticker)));
    const missingFromApi = watchlistLoaded ? [...activeUniverse].filter(t => !fromApiSet.has(t)) : [...new Set([...Array.from(pendingTickers).map(normalizeTicker).filter(Boolean), ...(watchlistTickers || []).map(normalizeTicker).filter(t => t && !fromApiSet.has(t))])];
    const placeholders = [...missingFromApi].filter(t => t && !fromApiSet.has(t)).map(t => ({
      ticker: String(t).toUpperCase(),
      sector: "Unknown",
      pct: 0,
      quality: 0,
      missing: [...TFS],
      tfs: {},
      coverage_pending: true
    }));
    const combined = [...enrichedFromApi, ...placeholders];
    const seen = new Set();
    const deduped = combined.filter(r => {
      const k = normalizeTicker(r.ticker);
      if (seen.has(k)) return false;
      seen.add(k);
      return true;
    });
    return deduped.sort((a, b) => (a.ticker || "").localeCompare(b.ticker || ""));
  }, [data, pendingTickers, watchlistLoaded, watchlistTickers, normalizeTicker]);
  const summaryStats = useMemo(() => {
    const total = watchlistLoaded ? new Set([...(watchlistTickers || []).map(normalizeTicker).filter(Boolean), ...Array.from(pendingTickers).map(normalizeTicker).filter(Boolean)]).size : data?.summary?.total_tickers_in_system || tickers.length;
    const withData = tickers.filter(t => Object.values(t.tfs || {}).some(tf => tf && tf.count > 0)).length;
    return {
      total,
      withData,
      noData: Math.max(0, total - withData)
    };
  }, [data, pendingTickers, tickers, watchlistLoaded, watchlistTickers, normalizeTicker]);
  const sectors = useMemo(() => {
    const s = new Set(tickers.map(t => t.sector).filter(Boolean));
    return ["all", ...Array.from(s).sort()];
  }, [tickers]);
  const filtered = useMemo(() => {
    let items = tickers;
    if (search) {
      const q = search.toUpperCase();
      items = items.filter(t => {
        if (t.ticker.includes(q)) return true;
        const name = getTickerName(t.ticker);
        if (name && name.toUpperCase().includes(q)) return true;
        const ind = getTickerIndustry(t.ticker);
        if (ind && ind.toUpperCase().includes(q)) return true;
        return false;
      });
    }
    if (sectorFilter !== "all") {
      items = items.filter(t => t.sector === sectorFilter);
    }
    items = [...items].sort((a, b) => {
      let va, vb;
      if (sortCol === "ticker") {
        va = a.ticker;
        vb = b.ticker;
      } else if (sortCol === "sector") {
        va = a.sector || "";
        vb = b.sector || "";
      } else if (sortCol === "industry") {
        va = getTickerIndustry(a.ticker) || "";
        vb = getTickerIndustry(b.ticker) || "";
      } else if (sortCol === "mcap") {
        va = getTickerMCap(a.ticker) || 0;
        vb = getTickerMCap(b.ticker) || 0;
      } else if (sortCol === "pct") {
        va = a.pct;
        vb = b.pct;
      } else if (sortCol === "price") {
        va = priceData?.prices?.[a.ticker]?.p ?? -1;
        vb = priceData?.prices?.[b.ticker]?.p ?? -1;
      } else if (sortCol === "change") {
        va = priceData?.prices?.[a.ticker]?.dp ?? -999;
        vb = priceData?.prices?.[b.ticker]?.dp ?? -999;
      } else if (TFS.includes(sortCol)) {
        va = a.tfs?.[sortCol]?.pct ?? -1;
        vb = b.tfs?.[sortCol]?.pct ?? -1;
      } else {
        va = a.ticker;
        vb = b.ticker;
      }
      if (typeof va === "string") {
        return sortDir === "asc" ? va.localeCompare(vb) : vb.localeCompare(va);
      }
      return sortDir === "asc" ? va - vb : vb - va;
    });
    return items;
  }, [tickers, search, sectorFilter, sortCol, sortDir, contextData, getTickerName, getTickerIndustry, getTickerMCap]);
  function handleSort(col) {
    if (sortCol === col) {
      setSortDir(d => d === "asc" ? "desc" : "asc");
    } else {
      setSortCol(col);
      setSortDir(col === "ticker" || col === "sector" ? "asc" : "desc");
    }
  }
  function sortArrow(col) {
    if (sortCol !== col) return "";
    return sortDir === "asc" ? " \u25B2" : " \u25BC";
  }
  function toggleSelect(ticker) {
    setSelected(prev => {
      const next = new Set(prev);
      if (next.has(ticker)) next.delete(ticker);else next.add(ticker);
      return next;
    });
  }
  function toggleSelectAll() {
    if (selected.size === filtered.length) {
      setSelected(new Set());
    } else {
      setSelected(new Set(filtered.map(t => t.ticker)));
    }
  }
  async function handleRemove(tickerList) {
    if (!tickerList || tickerList.length === 0) return;
    const confirmed = window.confirm(`Remove ${tickerList.length} ticker(s)?\n${tickerList.join(", ")}\n\nThis removes from the index (candle data in D1 is preserved).`);
    if (!confirmed) return;
    setActionStatus({
      type: "info",
      msg: `Removing ${tickerList.join(", ")}...`
    });
    try {
      const res = await fetch(`${API_BASE}/timed/watchlist/remove`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        credentials: "include",
        body: JSON.stringify({
          tickers: tickerList
        })
      });
      const json = await res.json();
      if (json.ok) {
        const removedList = (Array.isArray(json.removedTickers) ? json.removedTickers : tickerList).map(normalizeTicker).filter(Boolean);
        const removedSet = new Set(removedList);
        setWatchlistTickers(prev => (prev || []).filter(t => !removedSet.has(normalizeTicker(t))));
        setPendingTickers(prev => {
          const next = new Set(prev);
          removedList.forEach(t => next.delete(t));
          return next;
        });
        setWatchlistLoaded(true);
        setActionStatus({
          type: "success",
          msg: `Removed ${json.removed} ticker(s).`
        });
        setSelected(new Set());
        fetchData();
      } else {
        setActionStatus({
          type: "error",
          msg: json.error
        });
      }
    } catch (e) {
      setActionStatus({
        type: "error",
        msg: e.message
      });
    }
  }
  const [seqBackfill, setSeqBackfill] = useState(null);
  const seqCancelRef = useRef(false);
  const [scoreStatus, setScoreStatus] = useState({});
  async function handleRunOnboard(ticker) {
    const isTV = TV_TICKERS.has(ticker);
    if (isTV) return;
    setScoreStatus(prev => ({
      ...prev,
      [ticker]: "running"
    }));
    try {
      const res = await fetch(`${API_BASE}/timed/admin/onboard?ticker=${encodeURIComponent(ticker)}&skipBackfill=1`, {
        method: "POST",
        credentials: "include"
      });
      const json = await res.json();
      if (json.ok && (json.results?.[0]?.ok ?? json.ok)) {
        setScoreStatus(prev => ({
          ...prev,
          [ticker]: "ok"
        }));
        fetchData();
        setTimeout(() => setScoreStatus(prev => {
          const n = {
            ...prev
          };
          delete n[ticker];
          return n;
        }), 3000);
      } else {
        const err = json.results?.[0]?.error ?? json.error ?? "unknown";
        setScoreStatus(prev => ({
          ...prev,
          [ticker]: `error: ${err}`
        }));
      }
    } catch (e) {
      setScoreStatus(prev => ({
        ...prev,
        [ticker]: `error: ${e.message}`
      }));
    }
  }
  async function handleFillGaps(ticker) {
    const isTV = TV_TICKERS.has(ticker);
    if (isTV) return;
    if (ticker === "__SEQ_ALL__") {
      return handleFillAllSequential();
    }
    setBackfillStatus(prev => ({
      ...prev,
      [ticker]: "running"
    }));
    try {
      const endpoint = `${API_BASE}/timed/admin/alpaca-backfill?tf=all&ticker=${ticker}`;
      const res = await fetch(endpoint, {
        method: "POST",
        credentials: "include"
      });
      const json = await res.json();
      if (json.ok) {
        setBackfillStatus(prev => ({
          ...prev,
          [ticker]: "started"
        }));
        const pollDone = async () => {
          for (let attempt = 0; attempt < 60; attempt++) {
            await new Promise(r => setTimeout(r, 3000));
            try {
              const s = await fetch(`${API_BASE}/timed/admin/backfill-status`, {
                credentials: "include"
              }).then(r => r.json());
              const phase = s?.status?.phase;
              if (phase === "done" || !phase) break;
              setBackfillStatus(prev => ({
                ...prev,
                [ticker]: `${s.status.tf || ""} ${s.status.upserted || 0} bars`
              }));
            } catch {
              break;
            }
          }
          setBackfillStatus(prev => {
            const n = {
              ...prev
            };
            delete n[ticker];
            return n;
          });
          setBackfillStatus(prev => ({
            ...prev,
            [ticker]: "scoring..."
          }));
          try {
            const onRes = await fetch(`${API_BASE}/timed/admin/onboard?ticker=${encodeURIComponent(ticker)}&skipBackfill=1`, {
              method: "POST",
              credentials: "include"
            });
            const onJson = await onRes.json();
            if (onJson.ok && (onJson.results?.[0]?.ok ?? onJson.ok)) {
              setBackfillStatus(prev => ({
                ...prev,
                [ticker]: "done"
              }));
            }
          } catch (_) {}
          setBackfillStatus(prev => {
            const n = {
              ...prev
            };
            delete n[ticker];
            return n;
          });
          fetchData();
        };
        pollDone();
      } else {
        setBackfillStatus(prev => ({
          ...prev,
          [ticker]: `error: ${json.error || "unknown"}`
        }));
      }
    } catch (e) {
      setBackfillStatus(prev => ({
        ...prev,
        [ticker]: `error: ${e.message}`
      }));
    }
  }
  async function handleFillAllSequential() {
    if (seqBackfill?.running) return;
    seqCancelRef.current = false;
    const tickersToFill = (data?.tickers || []).filter(t => !TV_TICKERS.has(t.ticker) && (t.quality ?? t.pct) < 100).map(t => t.ticker);
    if (tickersToFill.length === 0) {
      setActionStatus({
        type: "success",
        msg: "All tickers already at 100% quality."
      });
      return;
    }
    const log = [];
    setSeqBackfill({
      running: true,
      current: tickersToFill[0],
      index: 0,
      total: tickersToFill.length,
      log,
      cancelled: false
    });
    for (let i = 0; i < tickersToFill.length; i++) {
      if (seqCancelRef.current) {
        log.push(`Cancelled at ${i}/${tickersToFill.length}`);
        setSeqBackfill(prev => ({
          ...prev,
          running: false,
          cancelled: true,
          log: [...log]
        }));
        fetchData();
        return;
      }
      const ticker = tickersToFill[i];
      setSeqBackfill(prev => ({
        ...prev,
        current: ticker,
        index: i,
        log: [...log]
      }));
      setBackfillStatus(prev => ({
        ...prev,
        [ticker]: "running"
      }));
      try {
        const res = await fetch(`${API_BASE}/timed/admin/alpaca-backfill?tf=all&ticker=${ticker}`, {
          method: "POST",
          credentials: "include"
        });
        const json = await res.json();
        if (!json.ok) {
          log.push(`${ticker}: error — ${json.error || "unknown"}`);
          setBackfillStatus(prev => ({
            ...prev,
            [ticker]: "error"
          }));
          continue;
        }
        let finalBars = 0;
        for (let attempt = 0; attempt < 120; attempt++) {
          await new Promise(r => setTimeout(r, 2000));
          if (seqCancelRef.current) break;
          try {
            const s = await fetch(`${API_BASE}/timed/admin/backfill-status`, {
              credentials: "include"
            }).then(r => r.json());
            const phase = s?.status?.phase;
            const upserted = s?.status?.upserted || 0;
            const tf = s?.status?.tf || "";
            const tfIdx = s?.status?.tfIndex || 0;
            const tfTotal = s?.status?.tfTotal || 9;
            finalBars = upserted;
            setBackfillStatus(prev => ({
              ...prev,
              [ticker]: `TF ${tf} (${tfIdx}/${tfTotal}) — ${upserted} bars`
            }));
            setSeqBackfill(prev => ({
              ...prev,
              progress: `TF ${tf} (${tfIdx}/${tfTotal}) — ${upserted} bars`,
              log: [...log]
            }));
            if (phase === "done" || !phase) break;
          } catch {
            break;
          }
        }
        log.push(`${ticker}: ${finalBars} bars`);
        setBackfillStatus(prev => {
          const n = {
            ...prev
          };
          delete n[ticker];
          return n;
        });
      } catch (e) {
        log.push(`${ticker}: error — ${e.message}`);
        setBackfillStatus(prev => ({
          ...prev,
          [ticker]: "error"
        }));
      }
    }
    setSeqBackfill(prev => ({
      ...prev,
      running: false,
      current: null,
      log: [...log]
    }));
    fetchData();
  }
  function TFCell({
    tfData
  }) {
    if (!tfData || tfData.count === 0) {
      return React.createElement("td", {
        className: "px-1 py-1 text-center",
        style: {
          minWidth: 52
        }
      }, React.createElement("div", {
        className: "text-[9px] text-gray-600"
      }, "\u2014"));
    }
    const q = tfData.quality != null ? tfData.quality : tfData.pct;
    const gapDays = tfData.gap_days;
    const hasGaps = gapDays != null && gapDays > 3;
    const freshHrs = tfData.freshness_hours;
    const isStale = freshHrs != null && freshHrs > 72;
    const tip = [`Count: ${tfData.count}/${tfData.expected}`, freshHrs != null ? `Freshness: ${freshHrs < 1 ? '<1h' : freshHrs < 24 ? Math.round(freshHrs) + 'h' : Math.round(freshHrs / 24) + 'd'} ago` : null, gapDays != null ? `Gap days (30d): ${gapDays}` : null, tfData.recent_dates != null ? `Trading dates (30d): ${tfData.recent_dates}` : null, `Quality: ${q}%`].filter(Boolean).join('\n');
    return React.createElement("td", {
      className: "px-1 py-1 text-center",
      style: {
        background: pctBg(q),
        minWidth: 52
      },
      title: tip
    }, React.createElement("div", {
      className: "text-[11px] font-semibold leading-tight",
      style: {
        color: pctColor(q)
      }
    }, q, "%", hasGaps && React.createElement("span", {
      className: "text-[8px] text-amber-400 ml-0.5",
      title: `${gapDays} gap days`
    }, "\u26A0")), React.createElement("div", {
      className: "text-[8px] leading-tight",
      style: {
        color: isStale ? '#ef4444' : '#6b7280'
      },
      title: formatDate(tfData.max_ts)
    }, formatAge(tfData.max_ts)));
  }
  return React.createElement("div", {
    className: "min-h-screen pb-16",
    style: {
      background: "var(--tt-bg-base)"
    }
  }, React.createElement("div", {
    className: "tt-page space-y-4"
  }, React.createElement("div", {
    className: "tt-card tt-card-pad"
  }, React.createElement("h1", {
    className: "tt-page-title"
  }, "Ticker Universe"), React.createElement("p", {
    className: "tt-page-lede"
  }, "Source of truth for the active universe. Add via Core Universe below; use Fill then Score on rows missing coverage."), (summaryStats.total > 0 || data?.summary) && React.createElement("div", {
    className: "flex flex-wrap gap-3 mt-4"
  }, React.createElement("div", {
    className: "tt-stat"
  }, React.createElement("div", {
    className: "tt-stat-label"
  }, "In System"), React.createElement("div", {
    className: "tt-stat-value"
  }, summaryStats.total)), React.createElement("div", {
    className: "tt-stat"
  }, React.createElement("div", {
    className: "tt-stat-label"
  }, "With Data"), React.createElement("div", {
    className: "tt-stat-value",
    style: {
      color: "var(--tt-up)"
    }
  }, summaryStats.withData)), React.createElement("div", {
    className: "tt-stat"
  }, React.createElement("div", {
    className: "tt-stat-label"
  }, "No Data"), React.createElement("div", {
    className: "tt-stat-value",
    style: {
      color: summaryStats.noData > 0 ? "var(--tt-dn)" : "var(--tt-up)"
    }
  }, summaryStats.noData)), React.createElement("div", {
    className: "tt-stat"
  }, React.createElement("div", {
    className: "tt-stat-label"
  }, "Showing"), React.createElement("div", {
    className: "tt-stat-value"
  }, filtered.length)))), React.createElement("div", {
    className: "tt-card tt-card-pad"
  }, React.createElement(CoreUniverseManager, null), React.createElement(UptickManager, null), React.createElement(MemberTickerListManager, null), React.createElement(SectorRatingsManager, null), React.createElement("div", {
    className: "tt-admin-section",
    style: {
      paddingTop: 12
    }
  }, React.createElement("div", {
    className: "flex items-center gap-3 flex-wrap"
  }, React.createElement("span", {
    className: "tt-admin-title",
    style: {
      color: "var(--tt-text-faint)"
    }
  }, "Admin Tools"), React.createElement("a", {
    href: "brand-kit.html",
    className: "tt-btn tt-btn--ghost",
    style: {
      textDecoration: "none"
    }
  }, "Brand Kit"), React.createElement("a", {
    href: "debug-dashboard.html",
    className: "tt-btn tt-btn--ghost",
    style: {
      textDecoration: "none"
    }
  }, "Debug Dashboard")))), React.createElement("div", {
    className: "tt-card tt-card-pad"
  }, React.createElement("div", {
    className: "flex flex-wrap items-center gap-3"
  }, React.createElement("button", {
    onClick: fetchData,
    disabled: loading,
    className: "tt-btn tt-btn--ghost"
  }, loading ? React.createElement(React.Fragment, null, React.createElement("span", {
    className: "tt-spin"
  }), "Loading\u2026") : "Refresh"), React.createElement("input", {
    type: "text",
    value: search,
    onChange: e => setSearch(e.target.value),
    placeholder: "Search tickers\u2026",
    className: "tt-input w-48"
  }), React.createElement("select", {
    value: sectorFilter,
    onChange: e => setSectorFilter(e.target.value),
    className: "tt-input"
  }, sectors.map(s => React.createElement("option", {
    key: s,
    value: s
  }, s === "all" ? "All Sectors" : s))), selected.size > 0 && React.createElement("div", {
    className: "flex items-center gap-2"
  }, React.createElement("span", {
    className: "text-xs",
    style: {
      color: "var(--tt-text-muted)"
    }
  }, selected.size, " selected"), React.createElement("button", {
    onClick: () => handleRemove([...selected]),
    className: "tt-btn tt-btn--danger"
  }, "Remove Selected"), React.createElement("button", {
    onClick: () => setSelected(new Set()),
    className: "tt-btn tt-btn--ghost"
  }, "Clear")), React.createElement("div", {
    className: "ml-auto flex items-center gap-2"
  }, React.createElement("button", {
    onClick: () => handleFillGaps("__SEQ_ALL__"),
    disabled: seqBackfill?.running,
    className: "tt-btn tt-btn--fill"
  }, seqBackfill?.running ? `Filling ${seqBackfill.index + 1}/${seqBackfill.total}…` : "Fill All Gaps"))), (seqBackfill?.running || actionStatus) && React.createElement("div", {
    className: "tt-progress mt-3"
  }, seqBackfill?.running && React.createElement("div", null, React.createElement("span", {
    className: "tt-spin"
  }), React.createElement("strong", null, seqBackfill.current), " \u2014 ", seqBackfill.index + 1, " of ", seqBackfill.total, seqBackfill.progress ? ` · ${seqBackfill.progress}` : "", React.createElement("button", {
    onClick: () => {
      seqCancelRef.current = true;
    },
    className: "tt-btn tt-btn--danger ml-3",
    style: {
      padding: "4px 10px",
      fontSize: 11
    }
  }, "Stop")), actionStatus && React.createElement("div", {
    style: {
      color: actionStatus.type === "error" ? "var(--tt-dn)" : actionStatus.type === "success" ? "var(--tt-up)" : "var(--tt-text-muted)"
    }
  }, actionStatus.msg, React.createElement("button", {
    onClick: () => setActionStatus(null),
    className: "ml-3 text-xs opacity-70 hover:opacity-100",
    style: {
      background: "none",
      border: "none",
      color: "inherit",
      cursor: "pointer"
    }
  }, "Dismiss"))), !seqBackfill?.running && seqBackfill?.log?.length > 0 && React.createElement("div", {
    className: "text-xs mt-2",
    style: {
      color: "var(--tt-text-faint)"
    }
  }, "Last fill run: ", seqBackfill.log.length, " tickers ", seqBackfill.cancelled ? "(stopped early)" : "completed")), error && React.createElement("div", {
    className: "bg-red-900/20 border border-red-800/40 rounded px-4 py-3 text-red-400 text-sm"
  }, "Error: ", error, React.createElement("button", {
    onClick: fetchData,
    className: "ml-3 underline text-xs"
  }, "Retry")), !loading && filtered.length > 0 && React.createElement("div", {
    className: "tt-card overflow-x-auto",
    style: {
      padding: 0
    }
  }, React.createElement("table", {
    className: "text-sm",
    style: {
      minWidth: 1700
    }
  }, React.createElement("thead", null, React.createElement("tr", {
    className: "bg-white/[0.03] border-b border-white/5"
  }, React.createElement("th", {
    className: "px-2 py-2 text-left w-8"
  }, React.createElement("input", {
    type: "checkbox",
    checked: selected.size === filtered.length && filtered.length > 0,
    onChange: toggleSelectAll,
    className: "accent-blue-500"
  })), React.createElement("th", {
    className: "px-3 py-2 text-left text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort("ticker")
  }, "Ticker", sortArrow("ticker")), React.createElement("th", {
    className: "px-2 py-2 text-left text-xs font-medium text-gray-400"
  }, "Name"), React.createElement("th", {
    className: "px-2 py-2 text-left text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort("sector")
  }, "Sector", sortArrow("sector")), React.createElement("th", {
    className: "px-2 py-2 text-left text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort("industry")
  }, "Industry", sortArrow("industry")), React.createElement("th", {
    className: "px-2 py-2 text-right text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort("mcap")
  }, "MCap", sortArrow("mcap")), React.createElement("th", {
    className: "px-2 py-2 text-left text-xs font-medium text-gray-400"
  }, "Groups"), React.createElement("th", {
    className: "px-2 py-2 text-center text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort("pct")
  }, "Avg", sortArrow("pct")), React.createElement("th", {
    className: "px-2 py-2 text-right text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort("price")
  }, "Price", sortArrow("price")), React.createElement("th", {
    className: "px-2 py-2 text-right text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort("change")
  }, "Chg% / As Of", sortArrow("change")), TFS.map(tf => React.createElement("th", {
    key: tf,
    className: "px-2 py-2 text-center text-xs font-medium text-gray-400 cursor-pointer hover:text-white select-none",
    onClick: () => handleSort(tf)
  }, TF_LABELS[tf], sortArrow(tf))), React.createElement("th", {
    className: "px-3 py-2 text-center text-xs font-medium text-gray-400"
  }, "Actions"))), React.createElement("tbody", null, filtered.map((t, idx) => {
    const isTV = TV_TICKERS.has(t.ticker);
    const bfStatus = backfillStatus[t.ticker];
    return React.createElement("tr", {
      key: t.ticker,
      className: `border-b border-white/[0.03] hover:bg-white/[0.02] ${selected.has(t.ticker) ? "bg-blue-900/10" : ""} ${t.coverage_pending || t.backfilling ? "bg-blue-900/5" : ""} ${t.needs_onboard && !t.coverage_pending ? "bg-amber-900/5" : ""} ${idx % 2 === 0 ? "" : "bg-white/[0.01]"}`
    }, React.createElement("td", {
      className: "px-2 py-1.5"
    }, React.createElement("input", {
      type: "checkbox",
      checked: selected.has(t.ticker),
      onChange: () => toggleSelect(t.ticker),
      className: "accent-blue-500"
    })), React.createElement("td", {
      className: "px-3 py-1.5"
    }, React.createElement("a", {
      href: `index-react.html#ticker=${encodeURIComponent(t.ticker)}`,
      className: "font-semibold text-blue-400 hover:text-blue-300 text-xs cursor-pointer hover:underline",
      title: `View ${t.ticker} details on Dashboard`
    }, t.ticker), (t.coverage_pending || t.backfilling) && React.createElement("span", {
      className: "ml-1.5 text-[9px] px-1 py-0.5 rounded bg-amber-900/40 text-amber-400",
      title: "Registry entry exists but candle coverage has not synced yet \u2014 use Fill then Score"
    }, "Sync pending"), t.needs_onboard && !t.coverage_pending && !t.backfilling && React.createElement("span", {
      className: "ml-1.5 text-[9px] px-1 py-0.5 rounded bg-orange-900/40 text-orange-300",
      title: "No candle data in D1 \u2014 run Fill (backfill) then Score to onboard"
    }, "Needs onboard"), isTV && !t.coverage_pending && !t.backfilling && !t.needs_onboard && React.createElement("span", {
      className: "ml-1.5 text-[9px] px-1 py-0.5 rounded bg-purple-900/40 text-purple-400"
    }, "TV")), React.createElement("td", {
      className: "px-2 py-1 text-[11px] text-gray-400 max-w-[140px] truncate",
      title: getTickerName(t.ticker)
    }, getTickerName(t.ticker)), React.createElement("td", {
      className: "px-2 py-1 text-[11px] text-gray-400 max-w-[100px] truncate",
      title: t.sector
    }, t.sector || "—"), React.createElement("td", {
      className: "px-2 py-1 text-[11px] text-gray-500 max-w-[130px] truncate",
      title: getTickerIndustry(t.ticker)
    }, getTickerIndustry(t.ticker) || "—"), React.createElement("td", {
      className: "px-2 py-1 text-[11px] text-right text-gray-500 tabular-nums whitespace-nowrap"
    }, (() => {
      const mcap = getTickerMCap(t.ticker);
      if (!mcap) return React.createElement("span", {
        className: "text-gray-600"
      }, "\u2014");
      const color = mcap >= 200e9 ? "text-blue-400" : mcap >= 10e9 ? "text-teal-400" : mcap >= 2e9 ? "text-gray-400" : "text-gray-500";
      return React.createElement("span", {
        className: color
      }, fmtMCap(mcap));
    })()), React.createElement("td", {
      className: "px-2 py-1"
    }, (() => {
      const grps = groupsForTicker(t.ticker);
      if (grps.length === 0) return React.createElement("span", {
        className: "text-[10px] text-gray-600"
      }, "\u2014");
      return React.createElement("div", {
        className: "flex flex-wrap gap-0.5"
      }, grps.map(g => {
        const wb = etfWeightBadge(t.ticker, g);
        return React.createElement("span", {
          key: g,
          className: "text-[9px] px-1 py-0 rounded bg-gray-800 text-gray-400",
          title: wb ? `${g} weight: ${wb}` : g
        }, g, wb ? React.createElement("span", {
          className: "ml-0.5 text-[#00e676]"
        }, wb) : null);
      }));
    })()), React.createElement("td", {
      className: "px-2 py-1.5 text-center",
      title: t.coverage_pending || t.backfilling ? "Waiting for coverage sync" : t.needs_onboard ? "No candles yet — Fill then Score" : `Count-based: ${t.pct}% | Quality: ${t.quality ?? t.pct}%`
    }, t.coverage_pending || t.backfilling ? React.createElement("span", {
      className: "text-[10px] text-amber-400"
    }, "\u2026") : t.needs_onboard ? React.createElement("span", {
      className: "text-[10px] text-orange-300 font-semibold"
    }, "0%") : React.createElement("span", {
      className: "text-xs font-bold px-2 py-0.5 rounded",
      style: {
        color: pctColor(t.quality ?? t.pct),
        background: pctBg(t.quality ?? t.pct)
      }
    }, t.quality ?? t.pct, "%")), (() => {
      const pd = priceData?.prices?.[t.ticker];
      const price = pd?.p;
      const chgPct = pd?.dp;
      const chg = pd?.dc;
      const pc = pd?.pc;
      const displayPrice = Number(price) > 0 ? price : Number.isFinite(pc) && Number.isFinite(chg) ? pc + chg : null;
      const isUp = chgPct > 0;
      const isDown = chgPct < 0;
      return React.createElement(React.Fragment, null, React.createElement("td", {
        className: "px-2 py-1 text-right text-[11px] text-white font-medium tabular-nums"
      }, displayPrice != null && displayPrice > 0 ? `$${Number(displayPrice).toFixed(2)}` : React.createElement("span", {
        className: "text-gray-600"
      }, "\u2014")), React.createElement("td", {
        className: "px-2 py-1 text-right text-[11px] tabular-nums"
      }, Number.isFinite(chgPct) && chgPct !== 0 ? React.createElement("div", null, React.createElement("span", {
        style: {
          color: isUp ? "#22c55e" : isDown ? "#ef4444" : "#9ca3af"
        }
      }, isUp ? "+" : "", chgPct.toFixed(2), "%", React.createElement("span", {
        className: "text-[9px] text-gray-500 ml-0.5"
      }, "(", isUp ? "+" : "", chg?.toFixed(2), ")")), (pd?.t > 0 || priceData?.updated_at > 0) && React.createElement("div", {
        className: "text-[9px] text-gray-600"
      }, formatAge(pd?.t || priceData?.updated_at))) : React.createElement("span", {
        className: "text-gray-600"
      }, "\u2014")));
    })(), TFS.map(tf => React.createElement(TFCell, {
      key: tf,
      tfData: t.tfs?.[tf]
    })), React.createElement("td", {
      className: "px-2 py-1.5 text-center"
    }, React.createElement("div", {
      className: "flex items-center justify-center gap-1 flex-wrap"
    }, (t.quality ?? t.pct) < 100 && (isTV ? React.createElement("span", {
      className: "text-[9px] text-gray-600",
      title: "TV-sourced: re-export from TradingView"
    }, "TV") : React.createElement("button", {
      onClick: () => handleFillGaps(t.ticker),
      disabled: bfStatus === "running" || bfStatus === "started" || bfStatus === "scoring...",
      className: "tt-btn tt-btn--fill",
      style: {
        padding: "4px 10px",
        fontSize: 10
      },
      title: typeof bfStatus === "string" && bfStatus.includes("bars") ? bfStatus : "Fill candle gaps via backfill, then auto-score"
    }, (bfStatus === "running" || bfStatus === "started" || bfStatus === "scoring...") && React.createElement("span", {
      className: "tt-spin"
    }), formatBfLabel(bfStatus))), !isTV && React.createElement("button", {
      onClick: () => handleRunOnboard(t.ticker),
      disabled: scoreStatus[t.ticker] === "running",
      className: "tt-btn tt-btn--score",
      style: {
        padding: "4px 10px",
        fontSize: 10
      },
      title: "Run scoring from existing candles (use after Fill)"
    }, scoreStatus[t.ticker] === "running" && React.createElement("span", {
      className: "tt-spin"
    }), formatScoreLabel(scoreStatus[t.ticker])), React.createElement("button", {
      onClick: () => handleRemove([t.ticker]),
      className: "px-2 py-0.5 text-[10px] bg-red-900/30 hover:bg-red-800/50 text-red-400 rounded",
      title: "Remove from index"
    }, "Remove"))));
  })))), loading && React.createElement("div", {
    className: "flex items-center justify-center py-20"
  }, React.createElement("div", {
    className: "text-gray-500 text-sm"
  }, "Loading coverage data...")), !loading && filtered.length === 0 && !error && React.createElement("div", {
    className: "flex items-center justify-center py-20"
  }, React.createElement("div", {
    className: "text-gray-500 text-sm"
  }, search || sectorFilter !== "all" ? "No tickers match your filters." : "No ticker data found."))));
}
const _AuthGate = window.TimedAuthGate;
const _tickerApp = _AuthGate ? React.createElement(_AuthGate, {
  apiBase: API_BASE,
  requiredTier: "admin"
}, user => React.createElement(App, {
  user: user
})) : React.createElement(App, null);
ReactDOM.createRoot(document.getElementById("root")).render(_tickerApp);
// cache-bust:1781903363395:978509879

// cache-bust:1781903363395:978509879
